#pragma once

#include <JGeometry.hpp>
class StarPointerTarget {
public:
    StarPointerTarget(f32, const TVec3f*, MtxPtr, TVec3f);
    u8 _0[0x1C]; // Target allocation size; fields remain unrecovered.
};
