# Reference-source provenance

Group_80247D70.cpp directly reconstructs two SB4E01 first-group wrappers with opaque layouts and exact helper dependencies. No external reference body copied. Details: enclosing work/holder-47d70/findings.md.

Group_80247CD0.cpp directly reconstructs two SB4E01 indexed group wrappers with opaque layouts and exact helper dependencies. No external reference body copied. Details: enclosing work/holder-47cd0/findings.md.

Iterate_80247BE0.cpp is a partial direct reconstruction of SB4E01 nested iteration with opaque layouts; it remains original-linked. No external reference body copied. Details: enclosing work/holder-47be0/findings.md.

Holder_80247B40.cpp directly reconstructs the SB4E01 constructor and array-runtime invocation with opaque layouts and exact callback dependencies. No external reference body copied. Details: enclosing work/holder-47b40/findings.md.

Nerve_80247A60.cpp directly reconstructs fourteen SB4E01 initialization/dispatch helpers with opaque layouts and exact vtable/branch dependencies. No external reference body copied. Details: enclosing work/nerve-47a60/findings.md.

Nerve_80247A00.cpp directly reconstructs SB4E01 startup through native static initialization, preserving opaque nerve identities and original constructor dependencies. No external reference body copied. Details: enclosing work/nerve-47a00/findings.md.

Cleanup_802479A0.cpp directly reconstructs the SB4E01 destructor wrapper, preserving opaque class identity and exact NameObj/delete dependencies. No external reference body copied. Details: enclosing work/actor-479a0/findings.md.

State_80247900.cpp is a partial direct reconstruction from SB4E01 with original shared-name dependencies; it remains original-linked. No external reference body copied. Details: enclosing work/actor-47900/findings.md.

Move_80247850.cpp directly reconstructs the SB4E01 matrix/blend routine with exact native calls and shared constant dependencies. No external reference body copied. Details: enclosing work/actor-47850/findings.md.

Move_80247760.cpp directly reconstructs the SB4E01 matrix/vector routine with exact native calls and shared constant dependencies. No external reference body copied. Details: enclosing work/actor-47760/findings.md.

Move_80247670.cpp directly reconstructs the SB4E01 sensor/vector movement routine with native sensor layout and exact dependencies. No external reference body copied. Details: enclosing work/actor-47670/findings.md.

Move_80247560.cpp directly reconstructs the SB4E01 target/vector movement routine with exact native dependencies and opaque identities. No external reference body copied. Details: enclosing work/actor-47560/findings.md.

State_802474B0.cpp directly reconstructs the SB4E01 first-step/clipping and velocity routine with exact native dependencies. No external reference body copied. Details: enclosing work/actor-474b0/findings.md.

Move_80247340.cpp directly reconstructs two SB4E01 movement/scalar routines with exact native dependencies and opaque identities. No external reference body copied. Details: enclosing work/actor-47340/findings.md.

Flag_80247290.cpp directly reconstructs the SB4E01 flag/query routine with exact argument and constant dependencies. No external reference body copied. Details: enclosing work/actor-47290/findings.md.

State_80247220.cpp directly reconstructs the SB4E01 distance/predicate state transition, preserving exact dependencies and opaque identities. No external reference body copied. Details: enclosing work/actor-47220/findings.md.

State_80247130.cpp directly reconstructs the SB4E01 conditional vector/state routine with opaque identities and exact dependencies. No external reference body copied. Details: enclosing work/actor-47130/findings.md.

State_80246FE0.cpp directly reconstructs the SB4E01 conditional state routine, preserving native vector return behavior and opaque dependencies. No external reference body copied. Details: enclosing work/actor-46fe0/findings.md.

Vector_80246E40.cpp directly reconstructs two SB4E01 vector routines with native temporary-return behavior and opaque dependencies. No external reference body copied. Details: enclosing work/actor-46e40/findings.md.

Vector_80246C40.cpp directly reconstructs the SB4E01 quaternion/vector routine with native vector operations and opaque helper identities. No external reference body copied. Details: enclosing work/actor-46c40/findings.md.

Sensor_80246BD0.cpp directly reconstructs the SB4E01 sensor/nerve wrapper, with opaque owning type and exact native dependencies. No external reference body copied. Details: enclosing work/actor-46bd0/findings.md.

Matrix_80246AF0.cpp partially adapts Petari TRotation3::setQuat (CC0, fa77cd4d2b1075dc37db55b7b7ef46b34cd9f9ce), with actor layout and dependencies reconstructed from SB4E01. It remains original-linked. Details: enclosing work/actor-46af0/findings.md.

Support_80246A20.cpp directly reconstructs two SB4E01 vector/conditional wrappers, preserving opaque identities and exact helper targets. No external reference body copied. Details: enclosing work/actor-46a20/findings.md.

Init_80246850.cpp is a partial direct reconstruction from SB4E01 actor-initialization instructions; it remains original-linked. No external reference body copied. Details: enclosing work/actor-46850/findings.md.

Actor_802467D0.cpp is directly reconstructed from SB4E01 constructor instructions, with opaque owning layout and verified helper/constant targets. No external reference body copied. Details: enclosing work/actor-467d0/findings.md.

BezierSection_802464E0.cpp adapts section selection and control-point indexing from Petari (CC0, fa77cd4d2b1075dc37db55b7b7ef46b34cd9f9ce), checked against SB4E01 instructions and linked dependencies. Details: enclosing work/bezier-464e0/findings.md.

BezierCoord_802462B0.cpp adapts four coordinate-search and iterator routines from Petari (CC0, fa77cd4d2b1075dc37db55b7b7ef46b34cd9f9ce), checked against SB4E01 helper targets and instruction order. Details: enclosing work/bezier-462b0/findings.md.

BezierSupport_802460A0.cpp adapts six normalization, length and position/direction routines from Petari (CC0, fa77cd4d2b1075dc37db55b7b7ef46b34cd9f9ce), verified against SB4E01 dependencies and instructions. Details: enclosing work/bezier-460a0/findings.md.

BezierInit_80245E80.cpp adapts the BezierRail constructor from Petari (CC0, fa77cd4d2b1075dc37db55b7b7ef46b34cd9f9ce), verified against SB4E01 including native array-constructor targets and accumulation order. Details: enclosing work/bezier-45e80/findings.md.

BezierNearest_80245D70.cpp adapts the sampling loop from Petari (CC0, fa77cd4d2b1075dc37db55b7b7ef46b34cd9f9ce); the paired-single distance helper is directly reconstructed from SB4E01 assembly. Details: enclosing work/bezier-45d70/findings.md.

BezierParam_80245C10.cpp is a partial adaptation of Petari BezierRailPart::getParam (CC0, fa77cd4d2b1075dc37db55b7b7ef46b34cd9f9ce). It remains original-linked. Details: enclosing work/bezier-45c10/findings.md.

BezierLength_80245A00.cpp adapts BezierRailPart::getLength from Petari (CC0, fa77cd4d2b1075dc37db55b7b7ef46b34cd9f9ce), verified against SB4E01 including the compiler-generated constant pool. Details: enclosing work/bezier-45a00/findings.md.

BezierRail.cpp direction helper and BezierRailPart::set, calcPos and calcVelocity are adapted from Petari (CC0, fa77cd4d2b1075dc37db55b7b7ef46b34cd9f9ce) and verified against SB4E01 instructions, constants and linked targets. Details: enclosing work/bezier-45620/findings.md and work/bezier-45850/findings.md.

Awake_802453C0 reconstructs flag setters, registration, native callback factory/dispatch/clone and cleanup directly from SB4E01 assembly. Unknown types remain opaque; no external reference body copied. Details: enclosing work/awake-453c0/findings.md.

Awake_80245220 reconstructs holder dispatch and awake-switch callback registration directly from local SB4E01 assembly. Shared callback layout remains opaque; no external reference body copied. Details: enclosing work/awake-45220/findings.md.

Iterate_802450C0 reconstructs native member-function range dispatch, callback return copying and holder allocation/insertion directly from SB4E01 assembly. No external reference body copied; class identity remains opaque. Details: enclosing work/iterate-450c0/findings.md.

Switch_80244F10 reconstructs switch callback initialization, initial/transition dispatch and holder construction from local SB4E01 instructions using the existing SwitchEventListener slots. No reference body copied; unknown owning class identities remain opaque. Details: enclosing work/switch-44f10/findings.md.

Nerve_80244E70/EB0 reconstruct native global singleton initialization and dispatch from local SB4E01 assembly, including the compiler-generated startup entry. No reference bodies copied. The startup symbol mapping is ABI bookkeeping, not extra progress. Details: enclosing work/nerve-44e70/findings.md.

Callback_80244BF0 reconstructs native member-pointer dispatch, heap cloning and five cleanup routines directly from local SB4E01 assembly. No external reference body copied; unknown class identities remain opaque. Details: enclosing work/callback-44bf0/findings.md.

Support_80244960 reconstructs eleven constructors, cleanup, scene/model wrappers and holder operations directly from local SB4E01 assembly, including inspected model dependencies. Unknown identities remain opaque; no external reference implementation copied. Details: enclosing work/support-44960/findings.md.

State_80244790 reconstructs two distance/switch/model state routines from local SB4E01 disassembly using the existing opaque View442B0 layout. No reference body copied; unknown helper semantics retain address labels. Details: enclosing work/state-44790/findings.md.

Support_802444F0 reconstructs seven callback-factory, lifecycle, initialization-wrapper and switch routines from local SB4E01 assembly. View442B0 remains an opaque layout name; both previously mapped callback bodies are now source-built. No reference implementation copied. Details: enclosing work/support-444f0/findings.md.

Init_802442B0 reconstructs initialization and native member-function callback construction from local SB4E01 instructions and data. View442B0 is an opaque layout name; its two address-named method mappings do not claim original class identity or source integration of those methods. No reference body copied. Evidence: enclosing work/init-442b0/findings.md.

Support_802440B0 reconstructs six allocation, cleanup, lifecycle callback and constructor routines directly from SB4E01 disassembly. No public reference body copied; unknown class identities remain opaque. LiveActor callback slot identities are verified from the linked vtable. Details: enclosing work/support-440b0/findings.md.

Matrix_80243CF0 is reconstructed directly from local SB4E01 disassembly using the existing JGeometry set<float> API. No reference implementation was copied; object identity remains opaque. Experiment and verification details: work/matrix-43cf0/findings.md in the enclosing workspace.

Matrix_80243F60 value-return expression helpers are derived from local target stack layout and constructor calls, using native C++ and previously documented symbolic-register paired primitives. No reference body was copied. Current experiments are recorded in the enclosing work/matrix-43f60/findings.md.

Shared math reconstruction uses SMGCommunity/Petari, CC0-1.0, commit fa77cd4d2b1075dc37db55b7b7ef46b34cd9f9ce.

Reference: https://github.com/SMGCommunity/Petari — src/Game/Util/MathUtil.cpp. Imported/adapted range-helper evaluation structure is annotated in source. All matching claims are verified against the local Galaxy 2 target, not inferred from Galaxy 1 status.

GameSystem and GameSystemObjHolder field identification, and the LCG routine, are informed by the same Petari revision. Only the accessed offsets are declared; Galaxy 2 machine code validates 0x20 and 0x34.

Further MathUtil reconstruction uses the same reference for easing, vibration, axis construction, vector normalization, and fixed-point conversions. Galaxy 2 disassembly determines signatures, call order, constants, and compiler-sensitive expression structure. JMATrigonometric sine-table indexing uses the existing angle-constant helper to reproduce the target multiplication order; each affected pre-existing exact function is checked for regressions.

The axis-angle TRotation3::setRotate implementation also adapts the Rodrigues formula expression structure from Petari's libs/JSystem/include/JSystem/JGeometry/TMatrix.hpp at the revision above. The Galaxy 2 target determines the vector copy, magnitude and normalization calls, double-precision trigonometric calls, and float conversions; the resulting 276-byte function matches exactly.

RailRider nearest-control-point reconstruction adapts the paired-single squared-distance primitive from Petari libs/JSystem/include/JSystem/JMath/JMath.hpp at the same CC0 revision. Navigation behavior, iteration order, and constants are recovered from the Galaxy 2 target.

LiveActorGroupArray reconstruction consults Petari src/Game/LiveActor/LiveActorGroupArray.cpp and include/Game/Util/JMapIdInfo.hpp for group-ID semantics and pointer-loop structure; the Galaxy 2 ABI requires value returns and a trivial implicit JMapIdInfo copy constructor. Target instructions determine inlining and comparison order.

MtxUtil rotation constructors adapt Petari src/Game/Util/MtxUtil.cpp at the same CC0 revision. Galaxy 2 disassembly determines arithmetic order and corrects the row 0, column 2 Euler-rotation expression.

MtxUtil local-axis rotations and pre-scaling overloads also adapt the same Petari reference. Galaxy 2 target instructions establish the X/Z rotation signs and degree-conversion operand order.

Coordinate-relative scaling follows the same Petari MtxUtil reference and uses a local forced-inline variant of the existing paired-single matrix-copy primitive. Temporary declaration order is recovered from target registers; the shared primitive is unchanged.

Directional scaling adapts the same Petari MtxUtil reference, with Galaxy 2 using the requested direction as the middle basis column. Its zero-translation and diagonal-scale helpers are reconstructed from target stores.

Translation/rotation/scale constructors and axis setters adapt the same Petari MtxUtil reference; scalar constructor expression and store order are adjusted against the Galaxy 2 target and remain partial.

Matrix flattening and quaternion rotation blending adapt the same Petari MtxUtil reference. Target calls determine projection operand order and quaternion helper ABI; unimplemented dependencies retain address names.

Combined matrix blending follows the same reference and the Galaxy 2 target: weighted translations, explicit sum-to-result assignment, and quaternion interpolation. Vector-operation dependencies retain address names pending their reconstruction.

Scale removal adapts the same Petari MtxUtil reference. Target calls preserve translation, extract and normalize each basis axis independently, and restore the basis. The getXYZDir helper uses the target float-set specialization.

Rotation/translation extraction follows the same reference with optional outputs, explicit matrix reference, and an optional radian-to-degree vector temporary. Euler extraction remains an address-named dependency.

Euler extraction consults Petari JGeometry/TMatrix.hpp getEulerXYZ at the same CC0 revision. Galaxy 2 disassembly and retail constants establish the singularity tolerance (2^-18), branch directions, output order, and angle-helper ABI. The original symbol name remains unresolved.

Side/up basis construction adapts Petari MtxUtil with explicit target cross-product calls and stack-local order.

Remaining two-direction basis constructors follow the same Petari MtxUtil family. Galaxy 2 disassembly determines cross-product operand order, including reversed operands relative to the reference for side/front, up/side and front/up, and inlined position variants.

Automatic-support basis constructors adapt the same Petari MtxUtil reference. Target calls establish support selection, float-set specialization, explicit cross products and the position variants calling the previously reconstructed basis routines.

Matrix X/Y turning wrappers and quaternion-to-basis conversion adapt the same Petari MtxUtil and JGeometry/TMatrix.hpp references. Galaxy 2 establishes helper ABI, degree conversion and preserved translation.

Local-direction matrix turning adapts the same Petari MtxUtil reference, with target matrix-vector evaluation and quaternion helper ABI.

Moment-to-matrix construction follows the same Petari MtxUtil reference. The TPosition3 rotation body reuses the already reconstructed Rodrigues-formula structure with a leading zeroTrans call. The vector isZero method uses the target squared-magnitude tolerance.

Matrix-axis comparison consults the same Petari MtxUtil reference. Galaxy 2 uses tolerance 2^-18 and calculates its relative axis from local Y transformed through inverse A then B, crossing local Y with that result; this differs from the reference local-Z sequence.

Matrix decomposition is recovered from Galaxy 2 target instructions: basis-column magnitudes, reciprocal-scale removal, and optional translation/Euler/scale outputs. Reciprocal uses the target fres primitive; unknown original names remain address-based.

The 408-byte fn_80046D50 is reconstructed directly from Galaxy 2 target instructions. It creates a diagonal column-length scale matrix, replaces near-zero scale reciprocals with zero using the verified 0.001 threshold, and concatenates the inverse scale with the source. Its original name remains unresolved.

fn_80046300 follows the CC0 Petari orthogonalize structure, checked against Galaxy 2 instructions. Target calls are preserved for cross products, normalization, the unused total magnitude, and inverse square roots. The original Z direction is restored after column normalization. It remains a partial instruction match.

The MR::sqrt<float> primitive and fn_800457F0 decomposition now match exactly. Source temporary declaration order fixes the square-root registers; loading column components and squaring them in place reproduces decomposition arithmetic.

TPosition3::makeRotate now matches its 296-byte target exactly. Separating declarations from assignments and ordering the component/cosine and square/complement temporary declarations reproduces the target floating-point registers and scheduling.

HitSensor constructor is adapted from the CC0 Petari structure and verified against all 180 target instruction bytes. Member initialization is expressed directly to reproduce the target stores without an out-of-line vector constructor. The entire 772-byte unit is now source-integrated; the following HitSensorInfo::setFollowPos symbol retains 16-byte alignment to preserve the retail layout.

LodCtrl constructor is reconstructed from Galaxy 2 target initialization and registration calls, consistent with CC0 Petari. All 172 constructor bytes match. Full unit integration also requires explicit template instantiation before the first caller, storing the unused Middle archive pattern as a 32-byte character array (not a pointer), and 16-byte alignment of the following function. Full retail executable byte equality is verified.

ScenarioData::getPowerStarNum now matches its 172 target bytes after making the field-name pointer explicit. ScenarioDataFunction::getScenarioDataParser is reconstructed directly from the 16-byte target accessor; partial local structures document only the verified 0x24 and 0x124 pointer offsets. Other root/holder identities remain unresolved.

ScenarioData iterator lookup is reconstructed as an inline search returning a copied JMapInfoIter, based on the Galaxy 2 stack-copy and loop instructions. File-level optimization recovers nested inlining. It now has 90.909096% similarity; null-branch block order remains different, so the unit is not integrated.

Scenario iterator now matches all 220 bytes: reversing the condition in scenarioLength reproduces the target null-branch order. The empty-string sdata split and following text alignment are recovered. Trial integration leaves only swapped compiler-generated Parser/Iter vtable order and references, so this unit remains NonMatching pending layout resolution.

ScenarioDataParser is now fully source-integrated. Declaring ScenarioDataIter before ScenarioDataParser (with a forward declaration) reproduces the target compiler-generated vtable order without changing function bodies or text order. All 2840 code bytes are exact and the full executable is byte-identical to retail.

NameObjHolder::callMethodAllObj now matches all 128 target bytes. Its pointer iteration agrees with CC0 Petari; the target member-pointer copies are reproduced by a local callable object and an inline loop returning that callable, with per-file optimization.

NameObjHolder cached lookup is reconstructed from target instructions, including move-to-front cache behavior, full-name predicate, and fixed-array insertion. The 104-byte find_if and 64-byte insert helpers match exactly. Lookup is 96.77419% similar; its full-cache path lacks one target count reload. The recovered cache type replaces opaque storage at offset 0xC without changing existing exact routines.

NameObjHolder lookup now matches all 372 bytes. Inline Vector::isFull and pop_back preserve the target count reload. The full 1052-byte unit is now integrated from source with retail byte equality.

LiveActor constructor and controller initialization follow CC0 Petari structure, checked against Galaxy 2 instructions. initRailRider (80 bytes) and initActorLightCtrl (68 bytes) match exactly; allocation sizes are 0x44 and 0x58. ActorLightCtrl storage remains opaque. The shared ClippingDirector interface replaces a local LodCtrl declaration. LiveActor constructor remains partial at 46.875% because vector initialization still emits calls instead of target stores. LiveActor remains NonMatching.

LiveActor::getTakingMtx (16 bytes), initBinder (184 bytes), and initShadowControllerList (84 bytes) adapt the CC0 Petari structure and now match the Galaxy 2 target exactly. initSound (132 bytes) is reconstructed from Galaxy 2 instructions, including the explicit copied offset vector passed to ActorSoundHolder. Target allocations establish Binder size 0x1F0, ActorSoundHolder size 0x14, and ShadowControllerList size 0x10. Unrecovered storage remains opaque.

LiveActor effect initialization and start/end clipping adapt the CC0 Petari structure and now match all 412 Galaxy 2 instruction bytes. Inline name/sensor accessors reproduce target argument evaluation and register allocation. EffectKeeper allocation is 0x34; fields remain opaque. The clipping-start effect dependency retains its unresolved fn_80233EE0 symbol rather than asserting an original name.

LiveActor appearance and death paths adapt CC0 Petari with Galaxy 2 target checks: makeActorAppeared (196 bytes), kill (104 bytes), and makeActorDead (160 bytes) now match exactly. Galaxy 2 kill additionally tests StageSwitchCtrl validity and the byte at 0x10 before calling onSwitchDead. ActorLightCtrl reset dependency retains the unresolved fn_8022C160 name.

LiveActor movement (284 bytes) and calcAnim (76 bytes) adapt the CC0 Petari control-flow structure with Galaxy 2 target controller ordering and now match exactly. Movement includes both ActorActionKeeper updateAnimKeeper and update calls, and retains address-based symbols for unresolved gravity/effect dependencies. calcAnim follows the target zero test of the existing flag at 0x72 despite its inherited field name. ModelManager declarations expose only the recovered interface.

LiveActor calcAnmMtx (92 bytes), getBaseMtx (72 bytes), calcViewAndEntry (36 bytes), and J3DModel::setBaseScale (28 bytes) now match exactly. Matrix access and animation ordering follow CC0 Petari structure with Galaxy 2 target checks. J3DModel partial layout records scale at 0x18 and base matrix at 0x24 only. The target view path repeats the no-calc-view check; this is retained. calcAnmMtx and scale setter stay out of line to preserve their callers.

LiveActor receiveMessage (144 bytes) and updateBinder (128 bytes) are reconstructed against Galaxy 2 target instructions and now match exactly. Dispatch uses push 0x33, player attack range 1..0x25, enemy attack range 0x5A..0x74, otherwise receiveOtherMsg. An initialized boolean result preserves the two enemy range comparisons. Binder returns a vector by value; the byte at actor offset 0x75 selects clear/add when nonzero, despite its inherited mIsOnBind field name.

LiveActor calcAndSetBaseMtx (164 bytes) adapts CC0 Petari and matches the Galaxy 2 target exactly, including taken-sensor host matrix and the Y-only rotation path. initActorStarPointerTarget (136 bytes) is reconstructed from target instructions: allocation 0x1C and by-value vector offset in both actor method and target constructor. Storage remains opaque.

LiveActor initModelManagerWithAnm now matches all 176 target bytes. Target allocation is 0x2C, with opaque ModelManager storage; initialization uses camera projection, model/animation setup, base scale and explicitly qualified base-matrix calculation, then ActorActionKeeper::tryCreate with the action name. The existing malformed constructor suffix PA4_C is completed to PA4_Cf, interpreting the projection input as a const float matrix pointer; constness is a source typing interpretation, not independently proven by machine instructions. Original-to-current symbol alias is recorded in checkpoint analysis.

LiveActor initActorCollisionParts now matches all 288 target bytes. It adapts CC0 Petari with the Galaxy 2 extra matrix pointer passed to the resource-holder factory; the existing declaration and symbol omitted this verified r7 argument and now include it. The local paired-single copy adapts the existing JMath primitive with reversed temporary declarations to reproduce target floating registers without changing other exact users.

LiveActor constructor now matches all 228 bytes. Per-unit scalar-vector constructor inlining reproduces target stores while default constructor attributes remain unchanged in other units. Per-unit out-of-line vector operator+= preserves the actual retail binder call target; objdiff instruction scoring had accepted its same-shaped add call before final-link verification exposed the relocation difference. All LiveActor functions now score exact, but source integration remains pending because the constant pool emits zero/one/minus-one instead of one/zero/minus-one. Trial integrations were reverted.

ActorSoundHolder wrappers at 0x8022CD70..0x8022CED8 are reconstructed directly from local Galaxy 2 instructions: nine functions / 304 bytes match exactly. Five pointer offsets preserve total holder size 0x14. Controller and entry layouts expose only accessed offsets and retain provisional type names. Unknown original function names remain address-based. The provisional text range 0x8022CB10..0x8022CEE0 groups the constructor and holder/actor wrappers; the following function operates on NerveExecutor and is excluded. The 596-byte constructor remains unreconstructed, and the unit is NonMatching.

ActorSoundHolder constructor is reconstructed directly from Galaxy 2 instructions and matches all 596 bytes. Allocation/initialization dependencies retain unresolved address names, while partial ResTable/ResourceHolder layouts expose only accessed fields. Explicit source-defined constants reproduce zero/0.001 ordering. The full 900-byte unit is now source-integrated; 16-byte alignment on following fn_8022CEE0 preserves boundary padding. Full retail executable hash and byte equality are verified. The local new header was avoided because it defines a non-inline placement-new function that duplicates an existing link symbol.

ActorStateBaseInterface::update is identified from the existing NerveExecutor-derived layout, dead byte at 0x8, virtual control slot at 0x1C, and CC0 Petari ActorStateBase.cpp at the documented revision. All 88 bytes match. The recovered name replaces fn_8022CEE0; its source unit covers 0x8022CEE0..0x8022CF40. Following ActorStateKeeper constructor alignment is 16 to preserve eight padding bytes. The unit is source-integrated with full retail hash and byte equality.

ModelManager constructor (52 bytes), update (156), newDifferedDLBuffer (8), six controller accessors (72), and resource accessor (8) are reconstructed from local Galaxy 2 disassembly: ten functions / 296 bytes score exact. Pointer offsets preserve the 0x2C allocation. Unresolved types and original function names remain opaque/address-based. fn_8023C0A0 is reconstructed but remains partial at 57.14286% due to branch layout. The provisional text range 0x8023B380..0x8023C540 remains NonMatching; this adds no source-integrated bytes.


2026-09-09 coordinator continuation: LiveActor is now source-integrated (3636 code bytes). A source-side getLiveActorDefaultScale helper returning literal one is used by the constructor and inlined under the existing per-file IPA configuration. Its emitted, unreferenced out-of-line body is normally dead-stripped; it arranges the literal pool as retail one/zero/minus-one without object patching or original-object reuse. The helper name is compiler-layout scaffolding, not an asserted retail function identity. All binder calls retain operator+= rather than the same-shaped add method. Central full retail SHA-1 and direct main.dol byte equality pass with the source object selected in the link graph. This supersedes earlier pending-integration notes.

ModelManager init (352), calcAnim (160), calcView (48), entry (48), and updateDL (300) are reconstructed directly from local Galaxy 2 target instructions, with exact instruction and external relocation symbol checks. No Petari adaptation was used for this batch. Opaque local dispatch slots reflect verified vtable offsets 0x0C/0x14/0x1C; the shared J3DModel declaration is unchanged. ModelManager remains NonMatching and contributes no integrated bytes.

NameObjCategoryList::remove now matches 108 instruction bytes: cache signed count before the array pointer and use an unsigned final-element index. No compiler pragma changes are required. The CategoryInfo destructor remains partial, so the unit stays NonMatching. HitSensorInfo::update now retains the follow-matrix pointer as a const reference, reproducing the missing reload after an aliased store; the remaining register difference keeps this function partial and its unit unintegrated.


ModelManager second and third continuation batches reconstruct 36 more functions / 2520 bytes directly from Galaxy 2 instructions, preserving verified external symbol operands. The fourth argument of fn_8023C3F0 is corrected from a provisional bool to an opaque player pointer, based on the null fallback to manager offset 0x18; its prior null caller remains unchanged.

NameObjCategoryList is now source-integrated: 1604 code bytes and 24 data bytes. CategoryInfo contains a 12-byte composed array/count container, adapting Petari's CC0 Vector composition at fa77cd4d and verifying Galaxy 2 count offset 0x8, callback 0xC, check count 0x10 and nested destructor branches. The local ListStorage name is provisional; shared MR::Vector is unchanged. Deferred destructor definition is selected only in this translation unit, and explicit delegator instantiation reproduces target weak-function order. The compiler-generated unused ListStorage destructor is stripped normally. Following NameObjExecuteInfo constructor at 0x8033A1F0 needs align:16 to retain eight padding bytes. Full retail hash and byte equality pass; this supersedes the earlier partial-category notes.


ModelManager FINAL integration, 2026-09-09: all 55 functions / 3964 code bytes in 0x8023B380..0x8023C460 are source-integrated, including fn_8023C0A0, init, calcAnim and updateDL. Switching on the pointer bits with only zero/default cases reproduces the three null-first accessor layouts on the verified 32-bit target. Explicit out-of-line helper declarations preserve calls after restoring retail function order. Owned source literals are the 8-byte "%s.arc" format at 0x80695658, empty string at 0x807CFAD0, and zero float at 0x807DC4D0; following alignment supplies the verified retail padding. The empty-string compiler label @383 resolves to the same source-owned retail address; no dependency function is accepted through a same-shaped substitute. Target/candidate instruction and relocation checks pass for all 55 functions, and source-link SHA-1 and direct DOL bytes pass.

ModelManager split now ends at 0x8023C460: the following routines invoke the LiveActor constructor, store a vtable and access a matrix at offset 0x90, identifying a separate actor object. Its 212 code bytes remain original-linked. This split correction adds one reporting unit (2160 to 2161) and is not counted as progress. ModelManager owns 24 bytes of data including padding. Source code, literals and alignment are generated normally; no retail object replaces this unit in the link graph. This supersedes all earlier NonMatching ModelManager notes.


ModelObj sequential reconstruction — 2026-09-09

All three methods in 0x8023C540..0x8023C6D0 are exact and source-integrated: constructor 276 bytes, init 16, calcAndSetBaseMtx 92. The 112-byte vtable at 0x806956D0 and zero float/padding at 0x807DC4D8 are also source-owned. Full retail SHA-1 and direct DOL equality pass, with the compiled ModelObj object selected and its original unit object absent from link inputs.

Petari CC0 fa77cd4d supplies the common ModelObj structure and matrix-update shape. Galaxy 2 differs materially: position is initialized before MR::initActor; the fallback scene/sound/effect path requires initActor false and mModelManager nonnull; defaults are draw9/movement0x22/calcAnim0xB; sound capacity is4 with explicit zero offset. These details follow retail instructions.

The existing ModelObj header and target constructor identity plus its verified vtable establish init/calcAndSetBaseMtx/destructor identities. Names replace fn_8023C660/fn_8023C670/fn_800BD0E0; aliases preserve historical comparisons. Relocation targets are checked, including correct set<float> specialization and the source-owned @644 zero float. All incoming exact functions remain exact.

An inline destructor definition makes the compiler emit the vtable. Its 92-byte destructor at 0x800BD0E0 is still supplied by the original strong definition in auto_03_800BB5D8_text.o. The emitted weak source destructor matches but is NOT counted as integrated. The claim is specifically384 source-integrated method bytes, not complete source ownership of all class dependencies.

Following fn_8023C6D0 requires align16; following sdata2 symbol807DC4E0 requires align8 to preserve trailing padding. Splitting text and owned data adds3 reporting units (2161 to2164); denominator changes are not progress.

Scratch destructor-only emission trials are in sequential-modelobj. Unused inline destructor with force_active emits no body; an inline destructor plus qualified-call wrapper folds into the wrapper; non-inline destructor emits a global vtable. None is integrated, and no destructor-byte gain is claimed.


2026-09-09 destructor integration supersedes the original-fallback note above. ModelObjDestructor.cpp defines the native C++ destructor with a TU-only novtable class annotation, so it emits no duplicate vtable. All92 instruction bytes and dependency symbols match. The original destructor range800BD0E0..800BD140 is now replaced by this source object; following fn800BD140 alignment16 preserves four padding bytes. Source object and absence of the original split object were checked in the final link inputs. Retail SHA-1 and direct DOL equality pass. No original-body fallback remains for this destructor. Its92 bytes are now counted as source-integrated.

# ShadowControllerList and vector support — sequential integration

All7 list routines /448 code bytes in8023EAD0..8023ECC0 are exact and source-integrated. The list's external vector constructor20 bytes at8023E8A0 and append helper32 bytes at8023F970 are also separately compiled and integrated at their retail locations. Following boundaries use verified16-byte alignment; no raw instruction blobs or original object substitution is used. Full retail SHA-1 and direct byte equality pass, and each source object replaces its original split object in link inputs.

List operations adapt Petari CC0 fa77cd4d ShadowController.cpp/ShadowController.hpp. Target allocation0x10 and accesses establish array0/capacity4/signed count8/hostC. An explicit specialization exposes only this concrete storage prefix and does not change the shared Vector implementation used by NameObjHolder. The signed count preserves target cmpwi for count==1 while unsigned loop indices preserve cmplw. The actual push_back symbol proves the container specialization identity; the referenced constructor's three zero stores establish its matching initialization.

ShadowController accessed fields remain opaque except host0/name4 and unnamed flags71/72. Other offsets and total size are not asserted. The request callback keeps fn_8023EDC0 until separately reconstructed. Get/add/reset/request method identities are supported by target operations and the public reference. No similarity-only matches are claimed.

Append requires a separate index=count++ statement before the array store to preserve target increment/dereference order. The initial array[count++]=controller form differed in20 executable bytes; it was corrected before acceptance.

This continuation also source-integrates the92-byte ModelObj destructor with the compiler's novtable annotation applied only in its separate TU. Total gain10 exact functions /592 code bytes. Four added source ranges split original metadata and add8 reporting units (2164 to2172); denominator changes are not progress.


# ShadowController — sequential candidate reconstruction

The new NonMatching unit covers 0x8023ECC0..0x8023F8B0. 49 functions / 2260 code bytes match instructions and literal external relocation operands after normalizing only local branch offsets. The request/update flow adapts local CC0 Petari fa77cd4d; remaining bodies follow retail target instructions and existing JGeometry operations. Pointer meanings remain opaque. The empty-string declaration uses the target symbol's verified four-byte size to select SDA addressing. The direction-update fallback preserves the target's paired vector copy and the original address-based gravity dependencies. Explicit noinline on fn_8023F490 preserves the retail call.

fn_8023EF90 is still absent; fn_8023F2C0 has a float-return scheduling mismatch. Source order and owned constant sections still need integration work. This entire unit remains original-linked; the 49 exact candidates add ZERO integrated code bytes. A passing DOL hash is not evidence of this unit's integration.

ShadowController projection continuation: fn_8023EF90 now matches332bytes with exact external operands. Its opaque result layout follows the target constructor's three words and seven vectors through offset0x54, and normal accessor stride12/baseC. The scaled-vector dependency retains its address name and uses verified hidden-return ABI. Source is now retail-ordered and 50/51 functions (2592bytes) are exact. fn_8023F2C0 and data integration remain unfinished; the unit still adds zero integrated bytes. This supersedes the previous missing-projection and source-order notes.


# ShadowController final source integration

All 51 functions / 2772 code bytes in 0x8023ECC0..0x8023F8B0 are now source-integrated. fn_8023F2C0 is exact with a transparent inline distance wrapper, which preserves the retail PSVECDistance relocation and yields the required f1 return placement. This helper is reconstruction scaffolding, not a claim about an original source helper name.

The unit owns shared shadow data at 807CFAE8..807CFAF0 (opaque pointer to 80695958 followed by the empty string) and 807DC500..807DC518 (1, 0, -1, 7000, 50, 0.001f). The preceding pointer is reconstructed from its actual relocation and retained as opaque void*. Grouping that verified pointer with the string preserves the eight-byte section alignment and actual string address CFAEC; it is not invented padding. A local sdata section pragma keeps initialized zero bytes in sdata rather than sbss. The 7000 constant and pointer are shared with surrounding shadow routines; ownership here does not claim those routines are decompiled.

The following addShadowController function is explicitly aligned to 16, preserving retail inter-function padding. Exact instruction operands and dependency symbols pass for all51 functions, all eight data objects and the pointer relocation match, the main link selects the source object and excludes the original unit, and the complete retail DOL SHA-1 and direct byte identity pass. All earlier NonMatching notes for this unit are superseded. The data partition increases reporting units by one, which is not progress.


# Shadow support and SensorGroup source integration

ShadowControllerFunction.cpp reconstructs three existing MR entry points at 8023F8B0..8023F970, 160 code bytes, directly from retail instructions. The holder view exposes only the verified prefix and concrete vector at offset14. The actor requests use the existing shadow-list pointer at7C, reset/request methods, and original opaque fn_80057470 predicate. No unverified predicate rename is introduced.

SensorGroup.cpp reconstructs the constructor/add/remove functions at 8023DEE0..8023DFE0, 236 code bytes. Constructor capacity is int as established by the original mangled symbol. The target accesses establish capacity0, count4, and pointer array8. Add writes the sensor back-pointer at1C; remove uses swap-with-last and preserves the target count reload. Add/remove adapt the local CC0 Petari fa77cd4d implementation; constructor initialization follows target disassembly. The unsigned last index reproduces target subtract/shift addressing.

Both units are source-linked with exact function instructions and dependency relocation operands, no original unit objects in the main link, and a full retail DOL byte-identity/SHA-1 pass. No incoming exact functions regress, including rebuilt HitSensor after its SensorGroup header gained the verified layout. Added source-integrated code is396bytes across six functions. SensorGroup partition adds one reporting unit; that denominator change is not progress.


# SensorGroup clear and SensorHitChecker integration

The inherited symbol at 8023DFE0 was mislabeled __ct__16SensorHitCheckerFPCc. Its actual operations traverse SensorGroup count4/array8 and zero HitSensor contact count14; no constructor initialization occurs. Reconstructed SensorGroup::clear() const matches52bytes and is now source-integrated. The original-to-corrected alias is recorded; this is new source reconstruction with byte verification, not naming counted as integration. SensorGroup now owns four routines/288bytes through8023E020.

SensorHitChecker owns nine routines/1828bytes at8023E020..8023E780 and96data bytes. Six group pointers at14..28 follow the verified NameObj prefix. Unresolved entry points retain address labels. The native C ABI definitions preserve target calls and manually declared virtual slots are validated against all retail table relocations. The string pool contains Player/Ride/Simple/MapObj/Character with target four-byte packing, and Eye occupies sdata with alignment supplying its padding. Only compiler section-base ...data.0 is normalized to the verified source-owned pool at80695900; no function dependency substitution is accepted.

Group selection, movement order, pair traversal and collision logic adapt local CC0 Petari fa77cd4d, corrected against Galaxy2 instructions and type constants. Same-group traversal begins the inner index at the outer index, differing from the reference's zero start. Declaring the two sensor locals before loop counters reproduces target register allocation. An inline radius-addition helper preserves the exact floating operand order; it is reconstruction scaffolding. Constructor and destructor follow target ABI, allocation, and base-call evidence.

All ten new routines (52+1828bytes) link from source with exact instruction/dependency checks, verified linked addresses, source-owned string/vtable data and a full retail DOL byte-identity/SHA-1 pass. No incoming exact matches regress. The following holder constructor alignment is16. This supersedes the previous boundary note that called DFE0 the start of SensorHitChecker.


# Shadow holder update unit integration

Four routines at8023E8C0..8023EAD0 (500 code bytes) now link from source. The holder view exposes verified registered vector14, pending vector20, current/default distances2C/30, and flags34/35. Controller clipping writes retain the verified73 offset through an opaque prefix. Movement/initial update loop structure adapts CC0 Petari fa77cd4d; Galaxy2 area-based clipping decisions and default restoration follow retail instructions and retain opaque address-based area helper names.

The native signed-int-to-float conversion emits an eight-byte bias at source rodata offset0, byte-identical to target80648AB8. Only this function references that target constant in the current reference audit; the earlier assumption it was shared with other routines was not supported. No arithmetic workaround remains. The anonymous compiler label @65 is normalized only to this verified source-owned constant.

The unit owns rodata80648AB8..80648AC0 and data80695990..806959E8: the two area-name strings and an eleven-slot virtual table. Every non-null virtual-table relocation is checked by symbol identity, including the still-original destructor dependency fn_8023F9F0. The separate constructor atE780 and destructor atF9F0 remain original-linked; this integration does not claim those routines. All four reconstructed instructions/dependencies, data bytes, linked addresses and source-object selection pass; the full retail DOL SHA-1 and direct byte identity pass without exact regressions. Source-integrated gain500code/96data bytes. Reporting partitions add two units, not progress.


# Shadow holder lifecycle continuation

The two destructor entry points at8023F990..8023FA70 (96+124=220bytes) are now source-integrated. The vector-storage destructor frees its pointer array and conditionally frees its own storage; the holder destructor visits pending20 then registered14 with deleting=-1, invokes the NameObj base destructor with0, then conditionally frees the holder. Both retain original address labels and exact dependency relocations. Following fn8023FA70 alignment16 preserves retail padding.

A shared ShadowHolderView header exposes the verified vtable pointer, opaque NameObj prefix and already-established holder fields. Existing holder update routines retain exact bytes after this declaration change. The unnecessary MSL_C/new include was removed from the destructor TU because its non-inline placement-new definition collided with an existing linked definition; no shared runtime header was modified.

The constructor at8023E780 is reconstructed but remains NonMatching: placement construction of the two vector members adds null checks absent from retail. It contributes no integrated bytes. The local inline placement operator avoids the header's duplicate definition; it does not resolve the checks. The full game remains incomplete, and only the two destructors are newly claimed as integrated. This supersedes earlier statements that both holder constructor and destructor remain original-linked.

ShadowControllerHolder constructor FINAL integration, 2026-09-09: native class construction removes the two spurious member-placement null checks and matches all 276 instruction bytes with the target dependency symbols. NameObj inheritance, the 11-entry vtable at 0x806959BC, vector members at 0x14/0x20, distances at 0x2C/0x30, and flags at 0x34/0x35 follow target instructions. The local CC0 Petari fa77cd4d ShadowController.hpp/.cpp corroborate class identity and the base/member constructor shape; Galaxy 2's two distance defaults, two flags, parameter calls and capacity values are reconstructed from the target. Only the verified constructor and vtable are renamed (__ct__22ShadowControllerHolderFv and __vt__22ShadowControllerHolder); original aliases are recorded. Other holder routines retain their address-based ABI labels and the explicit source-owned vtable retains their exact slot targets. No J3DModel changes.
The constructor owns the 56-byte parameter/name pool at 0x80695958..0x80695990, including the Shift-JIS NameObj name and parameter keys, emitted as ordinary string data. Exact source/target pool bytes and all instruction relocation operands, actual constructor/pool/vtable linked addresses, source-input selection with original constructor object absent, full DOL SHA-1 and direct byte identity are verified. Integrates 276 code bytes and 56 data bytes; naming alone is not counted. Assigning the formerly unnamed pool merges one data-only reporting unit (2179 -> 2178), which is not a progress gain. This supersedes the earlier placement-new partial-constructor notes.

ShadowDrawer base unit integration, 2026-09-09: reconstructs original fn_8023FA70/FAC0/FAD0 (constructor, controller getter/setter) directly from Galaxy 2 instructions, 84 code bytes at 0x8023FA70..0x8023FAE0. Local CC0 Petari fa77cd4d ShadowDrawer.cpp/.hpp corroborate behavior; target requires the Galaxy 2 NameObj prefix and controller pointer at 0x14. All address-based labels remain unchanged. Source-owned data at 0x806959E8..0x80695A18 contains the eleven-entry NameObj vtable and four padding bytes. Its exact destructor relocation remains fn_80095AA0; this unit does not claim reconstruction of that dependency. All three instruction sequences, external relocation symbols, vtable bytes and relocation targets, actual linked addresses and source-object selection are verified, with full retail DOL SHA-1 and direct bytes passing. Adds 84 integrated code bytes and 48 data bytes, with no incoming exact-function regressions.

ShadowSurfaceDrawer unit integration, 2026-09-09: four original-label routines at 0x80240270..0x80240450 (456 code bytes) reconstruct draw-init construction/callback registration, GX draw-state setup, surface-drawer construction and draw-init destruction. Local CC0 Petari fa77cd4d ShadowSurfaceDrawer.cpp/.hpp provide the high-level operation sequence; all Galaxy 2 constants, calling conventions, slot targets and instructions were reconstructed/checked against local target assembly. The callback is exposed only as its verified two-word vtable/function-pointer representation, retaining lbl_80681C10 and fn_8004B920. Assigning its two fields separately avoids MWCC's aggregate constant-copy code and exactly preserves target address relocations. GX dependencies retain original address labels; the packed color word is 0x00000080 on the verified big-endian target.
The two source-owned eleven-slot vtables at 0x80695AA0..0x80695AF8 total 88 bytes; destructor dependencies remain fn_8023FB40 for the surface base and fn_802403F0 for draw-init. fn_8023FB40 remains separately original-linked. Verification checks all four instructions/dependency symbols, both vtable relocation lists and raw bytes, actual linked addresses, source object selected/original unit absent, no incoming exact regressions, and full retail DOL SHA-1/direct bytes. Adds 456 integrated code bytes and 88 data bytes. Reporting-unit count increases by three from splitting existing unnamed text/data around the new unit; this is not decompilation progress.

ShadowSurfaceDrawer destructor integration, 2026-09-09: fn_8023FB40 (92 bytes, separate text range 0x8023FB40..0x8023FBA0) is reconstructed directly from target disassembly. Its second null guard is preserved by an inline base-cleanup helper before NameObj destruction; the helper name is reconstruction scaffolding, not a recovered retail symbol. Native compiler output matches both branch destinations, all instructions and the actual NameObj/operator-delete dependency symbols. No data is added. The existing source-owned surface vtable continues to reference fn_8023FB40 at the verified retail address, now supplied by source. Source selection/original-unit absence, linked address, all prior surface routines/vtable relocations, full DOL SHA-1 and direct bytes pass with no incoming exact-function regression. This supersedes earlier notes that fn_8023FB40 remained original-linked. The split adds two reporting units; that count change is not matching progress.

ShadowSurfaceCircle partial reconstruction, 2026-09-09: three routines (fn_80240060 constructor 76B, fn_802400B0 radius setter 8B, fn_80240210 destructor 88B) match all target instructions and dependency symbols, totaling 172 exact code bytes. The fn_802400C0 draw routine is reconstructed but remains partial (62.96296% diagnostic similarity); native temporary-vector layout and paired-single register allocation differ. Combined early-return predicates reproduce target branch structure. Local CC0 Petari fa77cd4d ShadowSurfaceCircle.hpp corroborates the interface; the function bodies are reconstructed from local target instructions. Address labels and opaque host/controller views remain. All referenced name/vtable/float data remains original-owned; no source integration is claimed for this NonMatching unit. The final link retains the original object and full DOL bytes pass with no incoming exact regression.

ShadowSurfaceCircle temporary-layout follow-up: native inline CirclePosition/circleOffset/circleNegated scaffolding reproduces the target vector stack offsets when the negated normal is a draw-call argument temporary. These helper names are reconstruction scaffolding, not recovered retail identifiers. Placement construction was rejected because it emits an extra null branch. The remaining center-pointer register lifetime and paired-single scheduling differences leave the draw routine at 62.96296% similarity. No new exact or integrated bytes are claimed; all three prior exact routines remain unchanged and the unit remains original-linked.

ShadowSurfaceCircle data audit: the source now defines 64 bytes at 0x80695A60..0x80695AA0 (Shift-JIS name, eleven-slot vtable plus padding), and two float literals at 0x807DC530..0x807DC538, verified as 1.0f and 100.0f directly from retail DOL bytes. Source/target data bytes and every vtable relocation target match. All original labels are retained. These 72 matching data bytes remain unintegrated because the circle unit is still NonMatching; source-integrated totals do not change. Applying register to the center aggregate had no effect and was removed.

ShadowSurfaceBox constructor integration: fn_8023FAE0, 84 source-built bytes at 0x8023FAE0..0x8023FB40, reconstructed directly from target assembly. Calls the exact surface-base constructor, installs original vtable lbl_80695A30, and initializes the verified three float fields at 0x18/0x1C/0x20 using lbl_807DC528. Original labels and opaque prefix are retained. Name, vtable and float remain external/original-owned; no data integration is claimed. Exact instructions and dependency symbols, actual linked function address, source object selection/original constructor unit absence, full DOL bytes and SHA-1 pass with no incoming exact regression. The other box routines remain unintegrated.

ShadowSurfaceBox remaining-unit reconstruction: original-label setter fn_8023FBA0 (28B) and destructor fn_80240000 (88B) exactly match target instructions and dependency symbols. The full 1088-byte draw routine fn_8023FBC0 is reconstructed from local target assembly at 71.32353% diagnostic similarity: it projects three model axes onto the normal plane, optionally multiplies half-extents by host scale, builds the draw matrix, chooses signed cube faces and writes twelve colored vertices to the GX FIFO. Native scalar component multiplication still differs from target paired-single multiplication; vector/matrix load order, temporary lifetimes and FIFO scheduling also differ. Helper names boxScaled/boxVertex are reconstruction scaffolding, not recovered names. Original data/function labels and opaque layouts are preserved; no reference source was copied for these bodies. The setter requires local declarations z,y,x followed by x,y,z loads to reproduce target FPR allocation; template and scalar set helper calls were rejected because they outline. This unit remains NonMatching and original-linked, adding 116 exact code bytes but no source-integrated bytes. Retail DOL byte identity and all incoming exact functions pass; the separately integrated constructor remains unchanged.

ShadowSurfaceBox draw follow-up: diagnostic similarity improves from 71.32353% to 97.79411%. Explicit matrix-column reads fix load order; named normal-projection temporaries fix stack lifetimes; an inline host-scale accessor removes the extra pointer move. Native sequential volatile GX FIFO writes replace the prior vertex argument wrapper, and opaque external float declarations avoid hoisting/reusing values across FIFO writes. No writes to those external floats are introduced and their constness is not claimed as recovered type information. Two small component-wise paired-single primitives are reconstructed in the existing JMath inline-assembly style, using symbolic compiler-assigned operands, with no hardcoded physical registers or embedded instruction words. All control flow, external dependency targets, geometry operations and FIFO emission are otherwise native C++. The remaining differences are three adjacent instruction-order swaps: setup pointers before multiply/add, and a literal load versus an integer argument load near the first quad. The draw routine remains NonMatching/original-linked, and there is no exact/integrated byte gain in this follow-up. Existing setter/destructor remain instruction-exact.

ShadowSurfaceBoxData integration: source emits the 24-byte Shift-JIS box name, 48-byte vtable/padding at 0x80695A18..0x80695A60, and five literal floats 1/0/0.5/-1/100 at 0x807DC518..0x807DC52C. Four following alignment bytes reproduce the retail range through 0x807DC530. Original labels and all vtable targets are retained, including still-original box draw/destructor dependencies. The data-only unit adds 96 integrated data bytes (92 emitted, four alignment), no code bytes. Raw section bytes, vtable relocation symbols, actual linked addresses, source/original input selection and full DOL SHA-1/direct equality are verified. No incoming exact functions regress.
Writable float definitions were rejected because the zero emitted to sbss2; the accepted definitions are const, with native sdata2 placement. Typed accessor experiments changed the still-partial draw routine's codegen and were discarded; its prior opaque read declarations are retained. That routine remains original-linked at 97.79411%, so data integration is not claimed as code integration.

ShadowSurfaceBox support integration: the previously exact setter fn_8023FBA0 (28B) and destructor fn_80240000 (88B) are now compiled/linked from separate source translation units. This adds 116 integrated code bytes and zero newly exact functions; the still-partial fn_8023FBC0 remains original-linked. No dependency alias or body substitution is used. Fresh per-unit instructions/relocations, actual linked addresses, source inputs with original support objects absent, full retail DOL bytes/hash and no incoming exact regressions are verified. Regression accounting canonicalizes the two moved functions to the previous unit so the split does not create artificial gains or losses. No data is added and no cumulative source is discarded.

ShadowSurfaceCircle support/data integration: the previously exact constructor fn_80240060 (76B), radius setter fn_802400B0 (8B), and destructor fn_80240210 (88B) now link from separate source units, adding 172 integrated code bytes and zero newly exact functions. The source-owned Shift-JIS name, vtable/padding and two floats add 72 integrated data bytes. Bodies, original labels and opaque layouts are unchanged. Fresh instructions, function dependency symbols, raw data and vtable relocations match; actual linked addresses, source selection/original support object absence, full DOL SHA-1 and direct byte equality pass. Regression accounting canonicalizes moved routines; no incoming exact function regresses. The 324-byte draw routine remains partial at 62.96296% and original-linked. Verification: work/shadow-circle-support-work/verify.py.

Circle draw helper follow-up: replaced the reconstruction-only CirclePosition aggregate with an ALWAYS_INLINE native TVec3f-return helper, keeping the negated normal as a draw argument temporary. Fresh disassembly shows the same mismatch and correct stack offsets as the containing-class form, at unchanged 62.96296% similarity. This removes an unnecessary invented aggregate; it does not add exact/integrated bytes. An attempted GNU-style asm label declaration for an explicit copy-constructor call was rejected by MWCC (global-register-variable syntax error), never generated a comparison object, and was removed. Do not repeat that unsupported declaration syntax. The persistent r31 lifetime difference remains unresolved; source support/data verification and full DOL bytes/hash still pass.

Circle paired-single follow-up: a TU-local circleAdd primitive, in the existing JMath inline-assembly style with symbolic compiler-assigned operands, fixes left/right Z-register allocation by declaring rightZ before leftZ. This improves draw diagnostic similarity from 62.96296% to 64.197525%. No fixed physical registers, embedded instruction words, new aliases or shared-header changes are used. The initial two-pointer helper with leftZ/rightZ order produced identical prior mismatches; reversed declaration order fixes the Z operands only. Center-pointer retention in r31 and instruction scheduling remain unresolved. Draw stays original-linked; exact-function and source-integrated byte counts are unchanged. Full DOL SHA-1/direct bytes and circle support instructions/data/relocations/source selection pass.

ShadowSurfaceOval support integration: reconstructed fn_80240450 constructor (132B), fn_802404E0 color setter (52B), fn_80240520 alpha setter (8B), and fn_80240530 size setter (20B) directly from target assembly, retaining original labels and an opaque prefix through 0x18. Local CC0 Petari fa77cd4d ShadowSurfaceOval.hpp corroborates only the interface; no reference function body was copied. Constructor writes model pointer18, size1C/20/24 and color28..2B; all external names/vtable/literals remain original-owned. Native packed-word color copy reproduces stack byte loads; direct member copy was rejected because it omitted the target stack copy. Existing setTVec3f supplies the exact size-copy primitive. Four new exact functions and 212 integrated code bytes, no data integration. Verification checks fresh instructions and dependency relocation symbols, actual linked addresses, source selected/original support object absent, no incoming exact regressions, full retail DOL bytes/SHA-1. Following draw/destructor remain original-linked.

ShadowSurfaceOval destructor integration: fn_802406E0, 88 bytes at 0x802406E0..0x80240740, reconstructed from target with native null guard, exact fn_8023FB40 base cleanup and conditional operator delete. Reuses the same verified C ABI body pattern as the circle destructor, retaining the original address label. One new exact function and 88 newly integrated code bytes; no data. Fresh disassembly compares instructions and actual dependency symbols; actual linked address, source input/original destructor object absence, no incoming exact regressions and full retail DOL hash/direct bytes pass. The oval draw and data still remain original-linked. Verification: work/shadow-oval-destructor-work/verify.py.

ShadowSurfaceOval draw integration: fn_80240550 (400B) reconstructed directly from target with combined projection predicates, host-scaled size, point/normal queries, color setup, flattened/scaled matrix and three model draws, then color reset. Native Vec-base copy avoids the target-absent TVec copy constructor call. Loading the denominator into a local before division matches float load order. Scalar host multiplication was rejected; a small TU-local ovalMultiply primitive in existing JMath inline-assembly style uses compiler-assigned symbolic operands (no fixed registers/instruction words). Operand declaration order hostXY,z,xy,hostZ matches allocation and retains size pointer across the host call. All 400 instructions-bytes and actual dependency symbols match. Source input selected/original draw object absent, actual linked address, no exact regressions, full retail DOL SHA-1/direct bytes pass. Adds one new exact function and 400 integrated code bytes, no data. All six oval routines now source-integrated (700 code bytes); names/vtable/literals remain original-owned and require separate audit. Helpers and opaque views are reconstruction scaffolding, not recovered type/name claims. Verification: work/shadow-oval-work/verify-draw.py.

ShadowSurfaceOval data integration: 104 source-owned bytes, comprising two 20-byte names (retail Shift-JIS display name and ShadowVolumeSphere resource), 48-byte vtable/padding at 0x80695B20, and floats 1/100/200/0.9 at 0x807DC538..0x807DC548. Data range0x80695AF8..0x80695B50. Definitions retain original labels; both raw sections and all vtable dependency symbols match the split original object. Actual linked addresses and source input/original data object absence, unchanged integrated code, no exact regressions, full DOL SHA-1/direct equality verified. All oval code700B and data104B now source-integrated, with dependencies outside those ranges separately owned. No new exact functions. Verification work/shadow-oval-data-work/verify.py.

ShadowVolumeBox support integration: three routines fn_80240740 (124B), fn_802407C0 (8B), fn_802407D0 (108B) reconstructed from target. Opaque layout preserves size0x24 and fourteen vertices0x30, unlike local CC0 Petari fa77cd4d interface reference with size1C/vertices28; no reference bodies copied. Constructor clears vertices via x=y=z assignment to emit target z/y/x stores; reverse chain was rejected. Size setter branches to the actual set<f> dependency; draw setup uses original GX/camera/vertex-builder symbols. All target instructions and dependencies exact, source selected/original support absent, linked addresses/full DOL bytes/hash verified, no incoming exact regressions. Adds three exact functions and 240 integrated code bytes; data and vertex-builder remain original-owned. Extra compiler-emitted weak set helper is not counted as an owned target function; verifier compares every target function and preserves its dependency identity.

ShadowVolumeBox data integration: source-owned 32-byte Shift-JIS name and 56-byte vtable at 0x80695B50..0x80695BA8, plus floats0/0.5/100 at 0x807DC548..0x807DC554 and four following alignment bytes through DC558. Adds104 integrated data bytes (100 emitted+4alignment), no code. All raw bytes and vtable relocation symbols exact, linked addresses/source selection/original data object absence/full DOL bytes/SHA-1 verified with no exact-function regressions. Several vtable functions remain original-linked: fn80241120,fn80241720,fn80240FD0,fn802416F0; preserved original symbols without claiming those dependencies integrated. No new exact functions. Verification work/shadow-volume-box-data-work/verify.py.

ShadowVolumeBox destructor integration: fn_80241120 (88B), target-native null guard, fn_80095A40(obj,0), conditional operator delete and object return. The base cleanup symbol remains original-linked; no substitute alias used. Fresh target/source instructions and dependency symbols exact; linked address, source selection/original destructor absence, full retail DOL SHA-1/direct bytes, no incoming exact regressions verified. Adds one exact function and 88 source-integrated bytes, no data. Existing vtable now reaches this source destructor at its retail address. Verification work/shadow-volume-box-destructor-work/verify.py.

ShadowVolumeDrawer support integration: six target-reconstructed routines fn_80241420 constructor112B, fn_80241490/14A0 setters8B each, fn_802414B0/14C0 toggles12B each, fn_802414D0 wrapper72B. Preserves target offsets18/1C/20 and opaque prefix, original labels, exact createSceneObj4E/connect2B dependencies. Local CC0 Petari fa77cd4d header corroborates interfaces only; no bodies copied, differing reference offsets not adopted. Native source matches all instructions and dependency symbols. Actual linked addresses, source selection/original support absence, full retail DOL bytes/hash and no exact regressions verified. Adds six exact functions224 integrated bytes. Vtable/literal and delegated fn_80241520 calculation remain original-owned. Verification work/shadow-volume-drawer-work/verify.py.

Volume position calculation fn_80241520 reconstructed at96.42857% diagnostic similarity:224B target, only adjacent addi r3,r1,14 / addi r4,r1,8 reversed. Native scaled/copy temporaries match stack and copy-constructor dependencies. A TU-local paired-single volumeAdd primitive (symbolic compiler-assigned operands, no fixed register/embedded words) matches arithmetic/FPR allocation; standard PSVECAdd differs in pointer/FPR allocation. Swapping helper argument order worsens pointer allocation and was reverted. Remains NonMatching/original-linked with zero new exact/integrated bytes. verify.py proves exactly one adjacent swap and every other instruction/dependency unchanged, no exact regressions/full retail DOL bytes.

Volume drawer length integration: fn_80241600 wrapper56B, fn_80241640 length144B, empty virtual hooks fn_802416D0/16E0 4B each, predicate fn_802416F0 36B reconstructed directly from target. Length queries controller, optionally replaces length with collision distance when cut flag and projection predicate true, then adds (-start+end). Local declarations end,start followed by start/end loads reproduce target FPR order; direct member expression gave swapped FPRs and was rejected. NO_INLINE on local calculation preserves exact wrapper dependency. All five functions244B instructions/dependency symbols match, source selected/original unit absent, actual linked addresses, full DOL bytes/hash and no incoming exact regressions verified. Adds five exact functions244 integrated bytes, no data. Empty bodies are evidenced by retail blr instructions, not placeholders. Position calculation remains partial/original-linked.

Volume drawer draw fn_80241720 integrated248B: target reconstructed enabled/load/shape virtual dispatch at slots34/2C/30, two cull/blend passes and color/alpha restoration. Explicit function-pointer table trial allocated r4 instead of target r12; native virtual view produces exact target code. Nine preceding virtual slots are opaque declarations used only to preserve verified offsets; no constructor/vtable emitted and no recovered names/types claimed. Used slots corroborated by target volume-box vtable. External color lbl807D34B8 remains original-owned. All instructions, direct dependencies and indirect slot offsets exact; actual linked address/source selection/original draw absence/full retail DOL bytes/hash/no regressions verified. Adds one exact function248 integrated bytes, no data.

Volume draw-init destructor fn_80241820 integrated88B. Target-native null guard, exact NameObj cleanup and conditional delete; original label retained. Initial source-link hash failed because following fn80241880 lacked explicit16-byte alignment; adding target-aligned boundary restored full DOL bytes. Fresh instructions/dependency symbols, actual linked address/source selected/original destructor absent/no exact regressions/full SHA-1 and direct bytes verified. Adds one exact function88 integrated bytes; no data. Verification work/shadow-volume-init-destructor-work/verify.py.

Volume draw-init constructor fn_802413B0 integrated104B. Target-native NameObj construction with name95C10, vtable95C68, callback pair80681C10/fn800220D0 registered at scene index2B. Reuses verified surface callback scaffolding with separate field assignments, retaining actual target symbols. Callback body/name/vtable remain original-owned. Fresh instruction/dependency symbols and actual linked address/source selection/original ctor absence/full DOL hash/direct bytes/no regressions pass. Adds one exact function104 integrated code bytes, no data. Verification work/shadow-volume-init-ctor-work/verify.py.

Volume drawer/init data integration:32-byte Shift-JIS name95C10,56-byte base vtable95C30,48-byte initializer vtable95C68,zero floatDC560 and4alignment bytes.144 integrated data bytes(140emitted+4alignment),no code. Both vtable relocation lists and raw sections match; actual addresses/source selection/original data object absence/full DOL bytes/hash/no exact regressions verified. Base destructor fn80095A40 remains original-linked; no claim that data integration integrates its dependencies. Original labels retained. Verification work/shadow-volume-data-work/verify.py.

Cylinder support integration: fn80241180 constructor92B,fn802411E0 destructor88B,fn80241240 radius setter8B. Target-reconstructed opaque layout radius28 differs from CC0 Petari fa77cd4d header20; reference interface only, no bodies copied. Original base/model/name/vtable/float symbols retained. Fresh instructions/dependency symbols/source selection/linked addresses/no regressions/full DOL bytes/hash verify three exact functions188 integrated code bytes, no data.

Cylinder draw-matrix fn80241250 integrated252B, target-reconstructed point/direction queries, paired negate, up matrix construction, radius/host scale and length scaling, camera concat and GX matrix load. Direct scale field assignments avoid extra scalar TVec constructor call; attempted macro-inline ctor did not remove it and was discarded. All instructions and dependency symbols, source selection/actual linked address/no regressions/full retail DOL bytes/hash verified. Adds one exact function252 integrated code bytes,no data. Position calculation dependency remains partial/original-linked; not counted by this integration.

fn_80241350 destructor integrated92B. Class identity remains opaque/address-labeled. Native inline base-cleanup helper preserves target second null guard and calls exact fn80095A40; conditional delete follows. Fresh instructions/dependency symbols/source selection/actual linked address/full DOL bytes/hash/no regressions verified. Adds one exact function92 integrated bytes,no data. Base cleanup remains original-owned. Verification work/shadow-destructor-41350-work/verify.py.

Cylinder data integrated112B(108emitted+4alignment): two24-byte names95BA8/95BC0,56-byte vtable95BD8,float100DC558 pluspadding. Vtable explicitly identifies fn80241350 destructor,fn80241250 matrix loader,fn80242150 shape,fn802416F0 predicate; shape remains original-linked. Raw sections, relocation symbols, actual linked addresses/source selection/full DOL bytes/hash/no regressions verified. No exact/code gains. This corroborates prior opaque destructor identity without renaming it. Verification work/shadow-cylinder-data-work/verify.py.

ShadowVolumeModel support integration: constructor fn802420C0 (68B), model loader fn80242110 (52B), and shape draw fn80242150 (8B) reconstructed directly from target. Opaque layout preserves model pointer0x24 and original labels. All target instructions and relocation symbols match; actual function/dependency addresses, source selection/original support absence, full retail DOL bytes/SHA-1 and no incoming exact regressions pass. Adds three exact functions and128 integrated code bytes, no data. Asset loader, model draw dependency and model vtable remain original-owned; their bytes are not counted as source integration. Verification work/shadow-model-work/verify.py.

ShadowSupport_80242160 integration: target-reconstructed constructor fn80242160 (104B) and predicate fn802421D0 (92B). Opaque prefix through0x28 and three scalar fields preserve target layout; original address labels retained. No reference bodies copied. Native short-circuit conjunction matches both predicate calls and branch layout. Every target instruction and dependency symbol, actual linked function/dependency addresses, source selection/original support absence, full retail DOL bytes/SHA-1 and no incoming exact regressions verified. Adds two exact functions196 integrated code bytes; names/vtable/constants remain original-owned and are not included in this gain. Verification work/shadow-support-42160/verify.py.

ShadowDraw_80242230 integration: 608-byte target reconstruction scales host size, clamps components, transforms projection direction, constructs and projects matrix axes, then loads the camera-concatenated matrix. All instructions and actual dependency symbols match, including matrix mult/setTrans, vector dot/set and JMAVECScaleAdd; no alias substitution. Opaque size offset0x28 and original address label retained. Reuses the already verified TU-local paired-single multiply scaffold with symbolic compiler-assigned operands; no fixed registers or instruction words. Initial candidate78.94737% differed only in local stack positions and matrix component load order. Declaring remaining vectors before zero temporary and explicitly loading each axis z/y/x produced exact target code. Source selection/original unit absence, all dependency linked addresses, no exact regressions, full retail DOL SHA-1 and direct byte equality verified. Adds one exact function608 source-integrated code bytes, no data. External matrix helpers/constants retain existing ownership; this does not claim their integration. No reference bodies copied. Verification work/shadow-draw-42230/verify.py.

ShadowSupport_80242490 integration: fn80242490 size setter20B,fn802424B0 destructor92B,fn80242510 constructor104B, reconstructed from target. Size uses existing paired-vector copy primitive at offset28. Destructor preserves both native null guards and original fn80095A40 cleanup. Constructor follows verified model-base construction pattern with distinct original name/vtable/float labels; unknown class identity stays opaque. No reference bodies copied. Fresh instructions and relocation symbols match all three functions; all dependency addresses, source selection/original support absence, no exact regressions and full retail DOL SHA-1/direct bytes verified. Adds three exact functions216 source-integrated code bytes, no data. Associated names/vtables/constants and base cleanup remain original-owned. Verification work/shadow-support-42490/verify.py.

ShadowDraw_80242580 integration: 668-byte target reconstruction queries the start position/direction, negates direction, scales/clamps size before host scaling, projects matrix axes and scales the up axis by volume length, then loads the camera-concatenated matrix. Reuses prior target reconstruction scaffolding, no reference bodies copied. Initial source differed only in four vector-copy FPR operands. TU-local modelCopy with symbolic compiler-assigned xy,z declarations resolves allocation; no fixed registers/embedded instruction words or shared-header edits. Host multiply load order follows this target. All instructions and actual dependency symbols/linked addresses exact; source selection/original unit absence, no exact regressions, full retail DOL SHA-1/direct equality verified. Adds one exact function668 integrated code bytes, no data. Constants and external dependencies retain existing ownership. Verification work/shadow-draw-42580/verify.py.

ShadowSupport_80242820 integration: size setter fn80242820 (20B), destructor fn80242840 (92B), constructor fn802428A0 (116B). Target reconstruction preserves opaque prefix, argument pointer24, zero pointer28 and floats2C/30/34. Native destructor retains two null guards and exact original fn80095A40 dependency. Existing paired vector-copy primitive matches setter. Original address labels and data dependencies retained; no reference bodies copied. Fresh instructions and dependency symbols/linked addresses, source selection/original support absence, no exact regressions and full retail DOL SHA-1/direct bytes verified. Adds three exact functions228 integrated code bytes, no data. Verification work/shadow-support-42820/verify.py.

ShadowDraw_80242920 integration: 204-byte target reconstruction selects explicit matrix pointer28 or host24 virtual slot40, copies/scales matrix and configures GX vertex input. Opaque virtual view has fourteen preceding slots plus matrix method, producing exact retail slot40 dispatch; no vtable emitted or class identity asserted. Target instructions, all direct dependency symbols/linked addresses and indirect dispatch offsets match. Source selection/original unit absence, no exact regressions, full retail DOL SHA-1/direct equality verified. Adds one exact function204 source-integrated code bytes, no data. No reference bodies copied. Verification work/shadow-draw-42920/verify.py.

ShadowDraw_802429F0 partial target reconstruction:612B geometry loop, diagnostic similarity85.62092%. Four trig queries establish bounds;32 sectors emit six GX vertices each. Native scalar FIFO stores, original address labels and opaque angle30/width34 retained; no reference bodies copied. Reversing outer multiplication expression leaves both fmuls operand-order mismatches unchanged. Remaining differences include emitted integer-conversion constant relocation @46 vs retail lbl80648AC0 and FIFO/arithmetic scheduling; do not alias away the relocation without proving ownership/address. Candidate remains NonMatching/original-linked, zero exact or integrated gains. Fresh equal instruction count, original link selection, unchanged exact/integrated totals, no regressions and full DOL byte identity verified. See work/shadow-draw-429f0/diff.txt and verify.py.

Geometry loop follow-up: separate ai/bi intermediate calculations before a/b scaling fix both fmuls operand orders and allocation. Diagnostic similarity now86.92811%. Compound-assignment trial changed FPR destinations incorrectly; inline position helper did not change scheduling and was removed. Remaining conversion constant relocation is @48 vs lbl80648AC0, plus FIFO scheduling. No exact/integrated gains; original selection/no regressions/full DOL byte identity verified.

Conversion constant audit: retail lbl80648AC0 was owned by auto_06_80648AC0_rodata.o; candidate local @48 contains exactly the same eight bytes4330000080000000. Assigned rodata80648AC0..80648AC8 to ShadowDraw_802429F0 split and verified both object sections byte-identical. No alias or source renaming used. Unit remains NonMatching/original-linked: matching constant bytes and ownership metadata do not prove source integration. Remaining FIFO scheduling still prevents exact function integration. Full DOL bytes/no regressions and unchanged integrated code verified.

FIFO scheduling audit: replacing scalar volatile FIFO pointer with volatile union member access produced identical instructions. Declaring lbl807DC5A8 volatile also produced identical instructions. Both experiments removed; simpler source retained. Similarity remains86.92811%, no exact/integrated gains. Original source-selection guard, unchanged exact/integrated accounting, no regressions and full DOL byte identity pass. These access-type hypotheses are ruled out; remaining scheduling requires another source-shape explanation.

ShadowSupport_80242C60 integration: wrapper fn80242C60 (8B), destructor fn80242C70 (88B), constructor fn80242CD0 (92B), reconstructed directly from target. Opaque pointer24 and scalar28 preserved; wrapper return represented as opaque32-bit result, no inferred semantic identity. Destructor uses exact fn80095A40 and positive-delete guard. Constructor uses original model base/name/vtable/constant symbols. All target instructions and dependency symbols/linked addresses, source selection/original unit absence, no exact regressions and full retail DOL SHA-1/direct bytes verified. Adds three exact functions188 integrated code bytes, no data. No reference bodies copied. Verification work/shadow-support-42c60/verify.py.

ShadowSupport_80242D30 integration: setter fn80242D30 (8B) and projection predicate fn80242D40 (92B), target reconstructed. Opaque scalar offset28 and original labels retained. Native short-circuit conjunction preserves exact branch layout and controller dependencies. All instructions and dependency symbols/linked addresses, source selection/original unit absence, no exact regressions and full retail DOL SHA-1/direct bytes verified. Adds two exact functions100 integrated code bytes, no data. No reference bodies copied. Verification work/shadow-support-42d30/verify.py.

ShadowDraw_80242DA0 integration:216-byte target reconstruction uses radius28 divided by original constant, optional host scale2C, explicit uniform matrix scale and projection translation, then camera concat/GX load. Original labels and opaque layouts retained. All target instructions and dependency symbols/linked addresses, source selection/original unit absence, no exact regressions and full retail DOL SHA-1/direct bytes verified. Adds one exact function216 integrated code bytes, no data. No reference bodies copied. Verification work/shadow-draw-42da0/verify.py.

ShadowDestructor_80242E80 integration:92-byte target destructor preserves two null guards, exact fn80095A40 base cleanup and positive conditional delete. Reuses verified native cleanup scaffold; original label retained, no class identity inferred or reference body copied. All instructions and dependency symbols/linked addresses, source selection/original object absence, no exact regressions and full retail DOL SHA-1/direct bytes verified. Adds one exact function92 integrated code bytes, no data. Verification work/shadow-destructor-42e80/verify.py.

ShadowCtor_80242EE0 integration:232-byte target reconstruction calls LiveActor base, optionally registers callback, initializes resource and caches model/data/item pointers90/94/98, then invokes target virtual slot38. Unknown argument and layouts remain opaque; J3DModel not altered. Three-word member aggregate assignment reproduces target lwzu and store scheduling; individual copies produced extra instruction. Native virtual view has twelve preceding slots, no emitted vtable. All instructions/dependency symbols/actual addresses and virtual slot match; source selection/original absence/no regressions/full retail DOL bytes/SHA-1 verified. Adds one exact function232 integrated code bytes, no data. No reference bodies copied. Verification work/shadow-ctor-42ee0/verify.py.

ShadowDraw_80242FD0 partial target reconstruction120B at93.333336% similarity. Copies resourceA0/A4/AC to global state10C/110/114, clears global62F0, dispatches model slot1C then calls exact data routines. Unknown layouts/types opaque; no J3DModel edits. Sole instruction difference is adjacent model-pointer load/global-clear store order. Changing global declaration from u32 to model pointer left difference unchanged and was reverted. No source integration: NonMatching/original-linked, unchanged exact/integrated totals, no regressions and full retail DOL bytes verified. No reference bodies copied. Verification work/shadow-draw-42fd0/verify.py.

ShadowDraw_80242FD0 now integrated120B: volatile model-pointer access alone left adjacent ordering mismatch; volatile global-clear plus volatile model-pointer read preserved retail store-before-load order and produced exact code. Qualifiers are local reconstruction access-order constraints, not claims of original declarations. No J3DModel change or fixed-register assembly. All target instructions/direct dependency symbols/actual addresses and virtual slot1C match; source selection/original unit absence/no regressions/full DOL SHA-1/direct bytes verified. Adds one exact function120 source-integrated bytes, no data. Verification work/shadow-draw-42fd0/verify.py.

ShadowCallback_80243050 partial48B at83.33333% similarity: native pointer-to-member invocation reproduces __ptmf_scall and nop, but target-pointer load occurs before LR save and member-address setup. Volatile target-pointer trial unchanged and removed. Class stays forward-declared/opaque. No exact/integrated gains; original-linked, unchanged exact/integrated accounting, no regressions and full retail DOL bytes verified. No reference bodies copied. Verification work/shadow-callback-43050/verify.py.

Callback scheduling follow-up: binding the member-function pointer to a const local reference before loading target produced identical instruction scheduling and unchanged83.33333% similarity. Removed redundant reference and retained direct invocation. No exact/integrated gains; original-linked/no regressions/full DOL byte identity verified. Do not repeat local-reference or volatile-target experiments.

ShadowClone_80243080 integration:104-byte target reconstruction allocates20 bytes from supplied opaque heap using exact __nw__FUlP7JKRHeapi dependency, checks null, installs original vtable and copies target/member words. Original labels and opaque types retained; no reference bodies copied. All target instructions and dependency symbols/linked addresses, source selection/original object absence/no regressions/full retail DOL SHA-1/direct bytes verified. Adds one exact function104 integrated code bytes, no data. Callback invocation remains partial/original-linked. Verification work/shadow-clone-43080/verify.py.

ShadowDestructor_802430F0 integration:92-byte target reconstruction preserves both null guards and exact NameObj destructor dependency, followed by positive-delete guard. Original label retained; no class identity inferred or reference body copied. Fresh instructions/dependency symbols/actual addresses, source selection/original object absence/no regressions/full retail DOL bytes/SHA-1 verified. Adds one exact function92 integrated code bytes, no data. Verification work/shadow-destructor-430f0/verify.py.

Ctor_80243330 partial296B target reconstruction at52.0% similarity. Base ModelObj construction, conditional host virtual40 attachment with two vector temporaries, scale stores and demo registration reconstructed. Opaque layout preserves scale2C/30/34 and flag9F; no reference bodies copied. Chained x=z assignment emits target z-before-x stores. Remaining main difference: compiler rematerializes string-pool base rather than retaining r29, changing saved registers/frame and associated scheduling. Unit remains NonMatching/original-linked, no exact/integrated gains; no regressions/full retail DOL byte identity verified. Target assembly work/after-spine.s; verification work/ctor-43330/verify.py.

Ctor43330 follow-up: local const char* pool retained across calls fixes frame/save registers and almost all instructions, improving similarity52% to97.2973%. Remaining difference: mr r5,r29 before mr r7,r30 vs target mr r7,r30 then addi r5,r29,0. Named struct-array fields at offsets0/12/28 did not change this and were removed; simple local pointer retained. No exact/integrated gains; original-linked/no regressions/full DOL bytes verified.

Ctor43330 declaration audit: const pointer-to-array with pool[0]/pool[1] yielded identical mr-vs-addi and scheduling mismatch. Reverted to simple const char* local. No exact/integrated gains; original selection/no regressions/full DOL byte identity verified. Array-pointer declaration joins struct-field trial as a ruled-out explanation.

Control_80243460 integration:164-byte target reconstruction conditionally switches named state and flag9F based on two opaque predicates. Native nested branches match target layout; original labels and opaque prefix retained. All instructions/dependency symbols/actual linked addresses, source selection/original object absence/no regressions/full retail DOL SHA-1/direct bytes verified. Adds one exact function164 integrated code bytes, no data. No reference bodies copied. Verification work/control-43460/verify.py.

Support_80243510 integration:92 bytes across two target routines. Wrapper preserves tail branch to fn8023CEF0; destructor preserves single null guard, fn800BAFC0 cleanup and positive conditional delete. Original labels and opaque types retained; no reference bodies copied. All target instructions and dependency symbols/linked addresses, source selection/original unit absence, no exact regressions and full retail DOL SHA-1/direct bytes verified. Adds two exact functions92 integrated code bytes, no data. Verification work/support-43510/verify.py.

Support_80243580 integration:144 bytes across constructor116 and index setter28. Constructor clears five fields, allocates256 pointer entries and zeros them; setter stores signed16 index and updates signed32 count. Opaque layout and original labels retained; no reference bodies copied. All target instructions/dependency symbols and actual linked addresses, source selection/original absence, no exact regressions and full retail DOL bytes/SHA-1 verified. Adds two exact functions144 integrated code bytes, no data. Verification work/support-43580/verify.py.

Init_80243620 integration:348-byte reconstruction computes record count from opaque resource entries, allocates five-byte flag records, clears them and assigns four control pointers to each LodCtrl. Resource offsets14/1C and entry20, Lod index2C derived from target loads; original labels retained and no reference bodies copied. Native loops and local five-byte record pointer reproduce all instructions. Exact setViewCtrlPtr dependency and linked address verified alongside all direct dependencies, source selection/original absence, no exact regressions and full retail DOL bytes/SHA-1. Adds one exact function348 integrated code bytes, no data. Verification work/init-43620/verify.py.

Support_80243780 integration:196 bytes across flag-pointer assignment76, verified empty hook4, and Lod registration116. Initial local Item pointer caused r7/r8 counter/item swap; direct group->items[i] expressions produce exact retail register allocation. Signed16 indices and five-byte flag layout follow target loads/multiply. Original labels/opaque types retained; no reference bodies copied. All target instructions and dependency symbols/actual addresses, source selection/original absence, no exact regressions and full retail DOL SHA-1/direct bytes verified. Adds three exact functions196 integrated code bytes, no data. Verification work/support-43780/verify.py.

Update_80243860 integration:296-byte target routine clears five-byte control records then tests each opaque area against player position through verified virtual slot2C and updates flags according to mode24/index20. Nine preceding native virtual methods preserve slot; no class identity or J3DModel change. Initial loop-local pointers swapped loop counter and record/area registers. Moving pointer declarations before their loops yields exact register allocation; direct repeated clear-field indexing instead produced redundant pointer reloads and was removed. All instructions/direct dependency symbols/actual addresses and virtual slot, source selection/original absence, no exact regressions and full retail DOL SHA-1/direct bytes verified. Adds one exact function296 source-integrated bytes, no data. No reference bodies copied. Verification work/update-43860/verify.py.

Ctor_80243990 integration:256-byte target constructor initializes opaque fields90..B4, color via original helper, creates two-word callback, initializes actor and scene connection. Initial aggregate callback initializer emitted extra constant-pool copy; explicit field assignments reproduce original symbol-address loads. Original labels and opaque types retained; no reference bodies copied. All instructions/dependency symbols and actual linked addresses, including callback function-address relocation fn800220D0, source selection/original absence, no exact regressions and full retail DOL bytes/SHA-1 verified. Adds one exact function256 integrated code bytes, no data. Verification work/ctor-43990/verify.py.

Support_80243A90 integration:112 bytes across five routines: virtual tail dispatch slot30, field94 setter, color copy through stack, field9C setter and vectorA0 setter. Ten preceding virtual methods preserve slot; opaque layout/original labels retained. Vector x/y/z initialized locals assigned f0/f1/f2; reverse initialized locals reversed loads. Inlined set helper also reversed loads/registers; removed. Declaring z,y,x separately then assigning x,y,z produces exact retail f2/f1/f0 and load order. All target instructions, virtual slot, source selection/original absence, no exact regressions and full retail DOL SHA-1/direct bytes verified; these functions have no direct external relocations. Adds five exact functions112 integrated bytes, no data. No reference bodies copied. Verification work/support-43a90/verify.py.

Fade_80243B20 partial reconstruction:164 code bytes across two frame-based step setters. Native signed-int conversion emits compiler-local @17 instead of shared original lbl80648AC8. All other instructions match. Assigned rodata80648AC8..80648AD0 to this unit; objdiff matches8 data bytes and both functions, but this is not relocation-verified integration. Trial Matching link fails because original fn80243F60 also references lbl80648AC8, absent from compiler output. Restored NonMatching/original object; cumulative source integration unchanged, no exact regressions and full retail DOL bytes/SHA-1 verified. Preserve native reconstruction and original-label constant ownership; next resolve shared conversion constant across fn80243F60 or a verified exported compiler-constant mapping. No reference bodies copied. Verification work/fade-43b20/verification.json.

Matrix_80243F60 partial324-byte reconstruction,54.651165% similarity. Identified shared conversion-constant user for Fade setters. Opaque view uses verified virtual slot40, scale2C and amountA0. Native reconstruction builds camera/model matrix, scales amount by integer index, subtracts from unit vector and prescales matrix. Initial28.395063% candidate evaluated model before camera and elided vector copy. Explicit camera local and const reference to unit-vector temporary restore target call order and separate copy, raising similarity. Remaining temporary offsets/vector paired operations and compiler-local shared conversion constant unresolved. Original-linked; no exact/integrated gain, no exact regressions and full retail DOL bytes/SHA-1 verified. No reference bodies copied. Verification work/matrix-43f60/verify.py.

Matrix43F60 follow-up:target-derived symbolic-register paired subtraction/multiplication helpers improve similarity54.651165% to67.90124%; declaring scale immediately after matrix moves its stack allocation to target2C and improves to72.83951%. Helpers use compiler-allocated registers, no fixed-register function body; no public reference bodies copied. Remaining other vector temporary offsets, copy-return register scheduling and shared compiler-constant mapping unresolved. Original-linked; exact/integrated totals unchanged, no exact regressions/full retail DOL bytes verified.

Matrix43F60 return-value trials: inline subtractResult returning copied/subtracted TVec3f scored70.37037%; nesting that result inside an inline multiplyResult passed directly to preScaleMtx scored67.90124%. Neither reproduces target temporary layout; restored72.83951% explicit-local candidate. These return-helper/NRVO expressions are ruled out in their tested forms. Original source selection and integration totals unchanged; full retail DOL bytes and no exact regressions reverified.

Update_80243BD0 integration:132-byte fade update adds step, clamps via original constants, then calls original predicate and show/hide dependencies. Clamp helper required always_inline to reproduce retail nested branch layout; default inline emitted out-of-line helper. No virtual dispatch in this routine; all three calls are direct. Original labels/opaque layout retained; no reference bodies copied. All instructions/dependency symbols/actual addresses, source selection/original absence, no exact regressions and full retail DOL bytes/SHA-1 verified. Adds one exact function132 source-integrated bytes, no data. Verification work/update-43bd0/verify.py.

Predicate_80243C60 integration:136-byte predicate checks optional host dead/clipped/hidden state and nonzero fade value. All native branches, instructions/dependency symbols/actual linked addresses, source selection/original absence, no exact regressions and full retail DOL bytes/SHA-1 verified. Adds one exact function136 integrated bytes, no data. Original labels/opaque fields retained; no reference bodies copied. Verification work/predicate-43c60/verify.py. Verified integrated total62832/6281816 exceeds1% milestone.

## Query_80247DF0

Direct SB4E01 reconstruction of the opaque group query and native vector-return helper, using existing JGeometry headers. No class identity or Petari implementation attribution inferred. Detailed evidence: work/holder-47df0/findings.md.

## Query_80248020

Direct retail reconstruction using the preceding opaque query layout and native JGeometry interfaces; no Petari body or owning class identity inferred. Partial evidence: work/holder-48020/findings.md.

## Query_80248270

Direct SB4E01 reconstruction using previously established opaque query layouts and native JGeometry interfaces, without inferred class identity or copied Petari body. Detailed evidence: work/holder-48270/findings.md.

## Query_802484D0

Direct SB4E01 reconstruction using opaque query layouts and native JGeometry vector operations, without inferred class identity or copied Petari body. Evidence: work/holder-484d0/findings.md.

## Query_80248790

Direct SB4E01 reconstruction of the opaque caller-buffer query using native vector interfaces; no inferred class identity or copied Petari body. Evidence: work/holder-48790/findings.md.

## Query_80248950

Direct SB4E01 reconstruction of the opaque caller-buffer query and its distinct helper dependencies, using existing native vector interfaces. No inferred class identity or Petari body copied. Evidence: work/holder-48950/findings.md.

## Bounds_80248AB0

Direct SB4E01 reconstruction of the shared bounds predicate using native vector layout. No inferred class identity or copied Petari body. Evidence: work/holder-48ab0/findings.md.

## Find_80248B40

Direct SB4E01 reconstruction of the nested-key lookup using observed opaque layouts. No inferred class identity or copied Petari body. Evidence: work/holder-48b40/findings.md.

## Access_80248BC0

Direct SB4E01 reconstruction of record access and lazy group allocation using observed opaque layouts. No inferred class identity or copied Petari body. Evidence: work/holder-48bc0/findings.md.

## Group_80248C70

Direct SB4E01 constructor, membership and insertion reconstruction with native vector initialization. Group48C70 is an opaque address-based name; fn_80248C70 maps to __ct__10Group48C70Fl. No Petari body copied. Evidence: work/group-48c70/findings.md.

## Bounds_80248D60

Direct retail reconstruction of group bounds/radius update using opaque layouts and native JGeometry operations, with no copied Petari body or inferred class identity. Partial evidence: work/group-48d60/findings.md.

## Bounds_80248F20

Direct retail reconstruction of the bounds check/update wrapper using opaque layouts and native vector copies. No copied Petari body or inferred class identity. Partial evidence: work/group-48f20/findings.md.

## Bounds_80249010

Direct SB4E01 reconstruction of bounds initialization/merge using opaque layout and native vector value/copy interfaces. No Petari implementation copied or class identity inferred. Evidence: work/group-49010/findings.md.

## Remove_802490F0

Direct SB4E01 reconstruction of group removal using opaque layout and a native inline range search. No inferred class identity or copied Petari body. Evidence: work/group-490f0/findings.md.

## Cleanup_80249170

Direct SB4E01 reconstruction of the destructor wrapper using native NameObj destruction and operator delete dependencies. No copied Petari body or inferred class identity. Evidence: work/holder-49170/findings.md.

## CollisionDirector

SB4E01 reconstruction with structural reference to Petari CollisionDirector.cpp (CC0 commit fa77cd4d2b1075dc37db55b7b7ef46b34cd9f9ce); retail five-holder count and layout follow original assembly. Existing native interfaces and original vtable/name exports retained. Evidence: work/director-49dd0/findings.md.

## Init_80249FB0

Direct SB4E01 initialization/group reassignment reconstruction using opaque layout and existing native matrix interfaces. No inferred class identity or copied Petari body. Evidence: work/item-49fb0/findings.md.

## Setup_8024A0F0

Direct retail setup reconstruction using opaque views and native vector copy ABI. A single volatile index read preserves observed snapshot ordering; original source qualifiers are unknown. No Petari body copied. Evidence: work/item-4a0f0/findings.md.

## Scale_8024A1C0

Direct SB4E01 matrix-column scalar reconstruction using native layouts and original scalar helper. No copied Petari body or inferred class identity. Evidence: work/item-4a1c0/findings.md.

## Group_8024A270

Direct SB4E01 registration/removal wrapper reconstruction using opaque views. Single volatile index reads preserve observed snapshots before director lookup; original qualifiers unknown. No Petari body copied. Evidence: work/item-4a270/findings.md.

## Flags_8024A330

Direct SB4E01 flag/setup and conditional-dispatch reconstruction. Retail argument preservation reveals an unused seventh setup parameter; existing setup body reverified unchanged. No Petari body copied. Evidence: work/item-4a330/findings.md.

## Matrix_8024A3A0

Direct SB4E01 matrix update/copy reconstruction using opaque item layout and native matrix interfaces. No copied Petari body or inferred class identity. Evidence: work/item-4a3a0/findings.md.

## Update_8024A4A0

Direct SB4E01 matrix/scale update reconstruction using opaque layout and native helpers. No copied Petari body or inferred class identity. Evidence: work/item-4a4a0/findings.md.

## Normalize_8024A5F0

Direct SB4E01 matrix-scale reconstruction using native vector/matrix types, original constants and observed flag branches. No copied Petari body or inferred class identity. Evidence: work/item-4a5f0/findings.md.

## Access_8024A7B0 and Name_8024A840

Direct retail scale setters/accessors with opaque layouts and native value ABI. Name accessor retained as a partial. No copied Petari body or inferred class identity. Evidence: work/item-4a7b0/findings.md.

## Query_8024A880

Direct SB4E01 query reconstruction with opaque result/predicate layouts and native matrix/vector interfaces. No copied Petari body or inferred class identity. Evidence: work/item-4a880/findings.md.

## Query_8024AA30

Direct SB4E01 swept-query reconstruction using native matrix/vector operations. Compiler-generated conversion constant owns original eight-byte rodata range; linked reference and bytes verified. No copied Petari body or inferred class identity. Evidence: work/item-4aa30/findings.md.

## Query_8024AD00

Direct SB4E01 query/result-loop reconstruction using opaque buffer layouts and native matrix/vector operations. No copied Petari body or inferred class identity. Evidence: work/item-4ad00/findings.md.

## Query_8024AEB0

Direct SB4E01 query reconstruction using native layouts/interfaces. Retained as an original-linked partial with register allocation unresolved. No Petari body copied. Evidence: work/item-4aeb0/findings.md.

## Query_8024AEB0 completion

Original direct-retail reconstruction refined with native array indexing; now source-integrated and exact. Supersedes prior partial status. Evidence: work/item-4aeb0-refine/findings.md.

## Project_8024B070

Direct SB4E01 geometry projection reconstruction using native vector operations and opaque primitive indices. No Petari body copied or original class identity inferred. Evidence: work/item-4b070/findings.md.

## Plane_8024B3E0

Direct SB4E01 projection-helper reconstruction using native vector copies, dot product, negation and scaling. No Petari body copied or original class identity inferred. Evidence: work/item-4b3e0/findings.md.

## Query_8024B4A0

Direct SB4E01 segment-query reconstruction with native vector operations and opaque result layouts. No Petari body copied or original class identity inferred. Evidence: work/item-4b4a0/findings.md.

## Query_8024B690

Direct SB4E01 query reconstruction using native matrix operations and opaque triangle storage. No Petari body copied or original class identity inferred. Evidence: work/item-4b690/findings.md.

## Query_8024B780

Direct SB4E01 array-query reconstruction using native matrix operations and opaque triangle storage. No Petari body copied or original class identity inferred. Evidence: work/item-4b780/findings.md.

## Displacement_8024B890

Direct SB4E01 displacement reconstruction using native matrix inversion and vector operations. No Petari body copied or original class identity inferred. Evidence: work/item-4b890/findings.md.

## Actor_8024B920

Direct SB4E01 derived-actor constructor reconstruction using an opaque layout and original exported vtable/scalars. No Petari body copied or original class name inferred. Evidence: work/actor-4b920/findings.md.

## Init_8024B9A0

Direct SB4E01 actor initialization reconstruction using the native LiveActor interface and original nerve storage. No Petari body copied or derived-class identity inferred. Evidence: work/actor-4b9a0/findings.md.

## Appear_8024BA00

Direct SB4E01 actor appearance reconstruction using native LiveActor/vector interfaces and original constants. No Petari body copied or derived-class name inferred. Evidence: work/actor-4ba00/findings.md.

## State_8024BAD0

Direct SB4E01 actor cleanup and nerve-setter reconstruction through native LiveActor interfaces. No Petari body copied or derived-class identity inferred. Evidence: work/actor-4bad0/findings.md.

## State_8024BB20

Direct SB4E01 target-position and nerve-predicate reconstruction using native vector and LiveActor interfaces. No Petari body copied or derived-class identity inferred. Evidence: work/actor-4bb20/findings.md.

## Step_8024BC80

Direct SB4E01 first-step actor reconstruction with original constant references and native vector operations. No Petari body copied or derived-class identity inferred. Evidence: work/actor-4bc80/findings.md.

## Step_8024BD20 candidate

Direct SB4E01 interpolation reconstruction with native helper calls. Original-linked pending shared conversion-constant resolution with fn_8024BED0. No Petari body copied. Evidence: work/actor-4bd20/findings.md.

## Step_8024BD20 completion

Direct-retail interpolation group extended through fn_8024BED0. Both conversion users and shared constant now source-integrated. Supersedes prior partial status. Evidence: work/actor-4bd20-complete/findings.md.

## Camera_8024BFF0

Direct SB4E01 camera-state reconstruction using native vector/camera interfaces and original constants. No Petari body copied or derived-class identity inferred. Evidence: work/actor-4bff0/findings.md.

## Cleanup_8024C0C0

Direct SB4E01 destructor-wrapper reconstruction preserving native base destructor and deallocation calls. No Petari body copied or class identity inferred. Evidence: work/actor-4c0c0/findings.md.

## Nerve_8024C120

Direct SB4E01 native startup reconstruction for five opaque nerve objects. No Petari body copied or original class identity inferred. Evidence: work/nerve-4c120/findings.md.

## Nerve_8024C170

Direct SB4E01 reconstruction of five opaque nerve constructors and five host-dispatch routines. Original vtable exports retained. No Petari body copied or class identity inferred. Evidence: work/nerve-4c170/findings.md.

## Actor_8024C210

Direct SB4E01 constructor reconstruction using an opaque derived-actor layout and original vtable/scalar exports. No Petari body copied or class identity inferred. Evidence: work/actor-4c210/findings.md.

## Init_8024C260

Direct SB4E01 actor initialization reconstruction using native LiveActor interfaces and original name/nerve storage. No Petari body copied or class identity inferred. Evidence: work/actor-4c260/findings.md.

## State_8024C2D0

Direct SB4E01 appearance and first-step state reconstruction using native LiveActor interfaces. No Petari body copied or class identity inferred. Evidence: work/actor-4c2d0/findings.md.

## Step_8024C380 candidate

Direct SB4E01 first-step reconstruction; original-linked with conversion register allocation unresolved. No Petari body copied or class identity inferred. Evidence: work/actor-4c380/findings.md.

### Matrix_8024C430 and Cleanup_8024C570
Direct SB4E01 assembly reconstruction; existing JGeometry symbolic vector helper reused. Matrix remains partial; cleanup accepted. Details in work/actor-4c430 and work/actor-4c570.

## Nerve_8024C5D0

Direct SB4E01 native startup reconstruction for three opaque nerve objects. No Petari body copied or original class identity inferred. Evidence: work/nerve-4c5d0/findings.md.

## Nerve_8024C610

Direct SB4E01 reconstruction of three opaque nerve constructors and two host-dispatch routines. Original vtable exports and the C380 body remain separately owned. No Petari body copied or class identity inferred. Evidence: work/nerve-4c610/findings.md.

## Nerve_8024C660

Direct SB4E01 reconstruction of an opaque Spine host dispatch into the source-built C310 state routine. No Petari body copied or class identity inferred. Evidence: work/nerve-4c660/findings.md.

## Name_8024C670

Direct SB4E01 reconstruction of two conditional name-copy paths and native stack/index ABI. Original table and helper bodies retained; no Petari body copied or original class identity inferred. Evidence: work/name-4c670/findings.md.

## Value_8024C740

Direct SB4E01 reconstruction of nine opaque tag/index routines, using native structure assignment. C750 pointer return corroborated by C8D0 caller. No Petari body copied or class identity inferred. Evidence: work/value-4c740/findings.md.

## Query_8024C7F0

Direct SB4E01 segment-proximity reconstruction using existing JGeometry vector methods and native floating comparison behavior. No Petari body copied. Evidence: work/query-4c7f0/findings.md.

## Actor_8024C8D0

Direct SB4E01 reconstruction of an opaque actor constructor, conditional helper allocations, matrix initialization and pointer array. No Petari body copied or original class identity inferred. Evidence: work/actor-4c8d0/findings.md.

## Init_8024CA30

Direct SB4E01 initialization reconstruction using native vector temporary lifetimes and existing LiveActor interface. No Petari body copied or class identity inferred. Evidence: work/init-4ca30/findings.md.

## Lifecycle_8024CB00

Direct SB4E01 reconstruction of three appearance/death routines, using existing LiveActor interface and opaque derived offsets. No Petari body copied or class identity inferred. Evidence: work/lifecycle-4cb00/findings.md.

## State_8024CC70

Direct SB4E01 reconstruction of four state-query/change routines using original nerve storage and existing LiveActor methods. No Petari body copied or original class identity inferred. Evidence: work/state-4cc70/findings.md.

## Change_8024CD60

Direct SB4E01 immediate value-change reconstruction; configuration layout verified from helper assembly and native u16 argument cast preserved. No Petari body copied or class identity inferred. Evidence: work/change-4cd60/findings.md.

## Control_8024CE30

Direct SB4E01 reconstruction of six control/forwarding routines. Native opaque child virtual slot and dependency behavior preserved without assigning class identities. No Petari body copied. Evidence: work/control-4ce30/findings.md.

## Control_8024CED0

Direct SB4E01 reconstruction of two conditional sound/child/nerve control routines using existing interfaces and original data. No Petari body copied or original class identity inferred. Evidence: work/control-4ced0/findings.md.

## Step_8024CFD0

Direct SB4E01 reconstruction of four setters and a timed actor state. Original helper calls, frame thresholds and data references preserved. No Petari body copied or class identity inferred. Evidence: work/step-4cfd0/findings.md.

## Step_8024D110

Direct SB4E01 timed-state reconstruction preserving helper order, flag branch and step150 transition. No Petari body copied or class identity inferred. Evidence: work/step-4d110/findings.md.

## Step_8024D220

Direct SB4E01 timed-state reconstruction including configuration at step39 and child appearance at40. No Petari body copied or class identity inferred. Evidence: work/step-4d220/findings.md.

## Matrix_8024D380

Direct SB4E01 matrix/child update reconstruction. Existing JGeometry copy/scale/set helpers reused; original constants and helper semantics verified. No Petari body copied or original class identity inferred. Evidence: work/matrix-4d380/findings.md.

### Setup_8024D580

Direct SB4E01 retail reconstruction of four child setup routines (604 code bytes), with offset-based actor fields and existing JGeometry helpers. No Petari adaptation or inferred original class identity. Retail strings/constants remain original-owned. Evidence: work/setup-4d580.

### Control_8024D7F0

Direct SB4E01 retail reconstruction of the flag/predicate callback routine (248 code bytes), using offset-based fields and retail nerve storage. No Petari adaptation or inferred original class identity. Evidence: work/control-4d7f0.

### Motion_8024D8F0 (partial)

Direct SB4E01 retail reconstruction of the 1648-byte rotation/input geometry routine. No Petari adaptation or inferred class identity. Candidate is 91.50485% similar with 37 instruction differences and remains original-linked; no integrated/exact gain. Evidence: work/motion-4d8f0.

### Children_8024DFE0 and Appear_8024DF60

Direct retail reconstruction of child visibility routines: DFE0/E030 source-linked (216 bytes). DF60 scores exact after compiler-private constant normalization but remains original-linked pending shared conversion-constant ownership (128 bytes, not integrated). No Petari adaptation or inferred original class identity. Evidence: work/children-4df60.

### Effects_8024E0C0

Direct retail reconstruction of five effect-dispatch routines (752 source-linked bytes), preserving offset-based fields, native effect strings and helper calls. No Petari adaptation or inferred original class identity. Evidence: work/effects-4e0c0.

### Helper_8024E3E0

Direct retail reconstruction of the 92-byte NerveExecutor helper constructor, preserving original name/vtable/nerve ownership and explicit native layout. No Petari adaptation or inferred original class identity. Evidence: work/helper-4e3e0.

### Factor_8024E440 (original-linked)

Direct retail reconstruction of two factor updates (288 bytes), exact after compiler-private constant-name normalization. Remains original-linked pending shared conversion-constant ownership; zero integrated gain. No Petari adaptation or inferred class identity. Evidence: work/factor-4e440.

### Helper_8024E560

Direct retail reconstruction of the helper constructor and three state routines (560 source-linked bytes). Preserves native layout, thresholds and nerve references. No Petari adaptation or inferred original class identity. Evidence: work/helper-4e560.

### Control_8024E7B0

Direct retail reconstruction of four child-control routines (664 source-linked bytes), preserving tag selection, signed remainder and nerve transitions. No Petari adaptation or inferred original class identity. Evidence: work/control-4e7b0.

### Cleanup_8024EA60

Direct retail reconstruction of three destructors (268 source-linked bytes), preserving null checks, native base cleanup and positive-flag deletion. No Petari adaptation or inferred original class identity. Evidence: work/cleanup-4ea60.

### Nerve_8024EB80

Direct retail reconstruction of thirteen nerve-object initializations:132 source-linked code bytes and60 integrated data bytes (.ctors plus .sbss including alignment). Original constructor helpers remain linked. No Petari adaptation or inferred class identity. Evidence: work/nerve-4eb80.

- `Nerve_8024EC10.cpp`: direct SB4E01 retail reconstruction of thirteen vtable constructors and thirteen host-dispatch/setter/no-op routines (320 code bytes); original vtables/constants retained. No Petari adaptation or inferred class identity. Evidence: `work/nerve-4ec10`.

- `Actor_8024EDB0.cpp`: direct SB4E01 retail reconstruction of constructor, state/effect/action methods and matrix update (15 routines, 628 code bytes). Uses existing LiveActor/JGeometry declarations; native constants and vtable retained. No Petari adaptation or inferred class identity. Evidence: `work/actor-4edb0`.

- `Cleanup_8024F080.cpp`: direct SB4E01 retail reconstruction of a 92-byte cleanup routine; native null checks, NameObj cleanup and conditional delete preserved. No Petari adaptation or inferred class identity. Evidence: `work/cleanup-4f080`.

- `Nerve_8024F0E0.cpp`: direct SB4E01 retail reconstruction of native four-object startup (60 code bytes, 20 integrated data bytes). Original helper targets/order and object addresses retained. No Petari adaptation or inferred class identity. Evidence: `work/nerve-4f0e0`.

- `Nerve_8024F120.cpp`: direct SB4E01 retail reconstruction of four vtable constructors and four host dispatch/action routines (224 code bytes). Native strings/vtables retained; no Petari adaptation or inferred class identity. Evidence: `work/nerve-4f120`.

- `Actor_8024F260.cpp` and `Lookup_8024F220.cpp`: direct SB4E01 retail reconstruction; constructor184 bytes source-linked, three-entry lookup retained as original-linked scheduling partial. Native table/vtable ownership preserved. No Petari adaptation or inferred class identity. Evidence: `work/actor-4f220`.

- `Init_8024F320.cpp`: direct SB4E01 retail reconstruction of actor initialization; original-linked partial with two vector temporary stack-offset differences among89 instructions. No Petari adaptation or inferred class identity. Evidence: `work/init-4f320`.

- `State_8024F490.cpp`: direct SB4E01 retail reconstruction of five lifecycle/state routines (220 code bytes), including child virtual slot30 and base kill. No Petari adaptation or inferred class identity. Evidence: `work/state-4f490`.

- `State_8024F580.cpp`: direct SB4E01 retail reconstruction of nerve selection and two-case dispatch (40 code bytes); native targets/arguments retained. No Petari adaptation or inferred class identity. Evidence: `work/state-4f580`.

- `Positions_8024F5B0.cpp` and `Query_8024F6F0.cpp`: direct SB4E01 retail reconstruction; position loops remain original-linked partial, constant-false query8 bytes source-linked. Existing LiveActorGroup/vector declarations reused; no Petari adaptation or inferred class identity. Evidence: `work/positions-4f5b0`.

- `Setup_8024F700.cpp`: direct SB4E01 retail reconstruction of array and child setup (172 code bytes), retaining native construction runtime, constructor helper and allocation sizes. No Petari adaptation or inferred class identity. Evidence: `work/setup-4f700`.

- `Group_8024F7B0.cpp`: direct SB4E01 retail reconstruction of group/array/callback setup; original-linked partial with12 callback-copy differences among81 instructions. No Petari adaptation or inferred class identity; callback payload remains opaque. Evidence: `work/group-4f7b0`.

- `Callbacks_8024F900.cpp`: direct SB4E01 retail reconstruction of four-callback setup; original-linked partial, functional call order retained, frame/register/shared-pool addressing unresolved. No Petari adaptation or inferred class identity. Evidence: `work/callbacks-4f900`.

- `Callback_8024FA20.cpp`: direct SB4E01 retail reconstruction of callback payload copy; original-linked partial with two swapped instruction positions among16. Explicit opaque temporary retained; no Petari adaptation or inferred class identity. Evidence: `work/callback-4fa20`.

- `Setup_8024FA60.cpp`: direct SB4E01 retail reconstruction of four child/helper setup routines (324 code bytes), retaining allocation sizes, field offsets and native initialization calls. No Petari adaptation or inferred class identity. Evidence: `work/setup-4fa60`.

- `Setup_8024FBB0.cpp`: direct SB4E01 retail reconstruction of four child/record-array setup routines (420 code bytes); native sizes, runtime callbacks, positions and scale preserved. No Petari adaptation or inferred class identity. Evidence: `work/setup-4fbb0`.

- `Group_8024FD70.cpp`: direct SB4E01 retail reconstruction of constructor forwarding and group appear loop (112 code bytes); native constructor/name and virtual slot retained. No Petari adaptation or inferred class identity. Evidence: `work/group-4fd70`.

- `Control_8024FDF0.cpp`: direct SB4E01 retail reconstruction of conditional child-control routine (136 code bytes); repeated predicates, field offsets and call order preserved. No Petari adaptation or inferred class identity. Evidence: `work/control-4fdf0`.

- `Query_8024FE80.cpp`: direct SB4E01 retail reconstruction of three-predicate short-circuit query (108 code bytes); native field/call order retained. No Petari adaptation or inferred class identity. Evidence: `work/query-4fe80`.

- `Control_8024FEF0.cpp`: direct SB4E01 retail reconstruction of pointer-transition control (176 code bytes). Native pointer tests, helper order and child appear virtual preserved; no Petari adaptation or inferred class identity. Evidence: `work/control-4fef0`.

## Group_8024FFA0

Direct SB4E01 retail reconstruction of fn_8024FFA0, fn_80250000, fn_80250060 and fn_802500C0: four group iteration/helper dispatch routines, 352 exact source-integrated bytes. No Petari adaptation or inferred class identity. Evidence: work/group-4ffa0/findings.md and verification.json.

## Array_80250120 and Select_80250240

Direct SB4E01 retail reconstruction of fn_80250120, fn_80250180, fn_802501E0 and fn_80250240: three fixed-count record operations and one signed candidate-selection routine, 408 exact source-integrated bytes. No Petari adaptation or inferred original class identity. Evidence: work/array-50120/findings.md and verification.json.

## Control_802502C0

Direct SB4E01 retail reconstruction of fn_802502C0 and fn_80250360: state/action dispatch and previous-child cleanup, 228 exact source-integrated bytes. Explicit shared-block branches reproduce native code; no claim of original author syntax or class identity. No Petari adaptation. Evidence: work/control-502c0/findings.md and verification.json.

## Display_802503B0

Direct SB4E01 retail reconstruction of fn_802503B0, 444 exact source-integrated bytes: record/name/calendar-like buffer and display descriptor setup. No Petari adaptation or inferred original class identity. Native caller evidence refines the Value_8024C740 pointer return and Control_8024FEF0 integer argument; both remain instruction-exact with zero additional counted gain. Evidence: work/display-503b0/findings.md and verification.json.

## Positions_80250570

Direct SB4E01 retail reconstruction of fn_80250570, fn_802505C0 and fn_80250610: predicate, selected-position setup and three-position update, 372 exact source-integrated bytes. Shared retail float constants retain existing ownership; emitted weak vector add helper excluded from gain. No Petari adaptation or inferred original class identity. Evidence: work/positions-50570/findings.md and verification.json.

## Create_802506F0

Direct SB4E01 retail reconstruction of fn_802506F0,148 exact source-integrated bytes: formatted name allocation, actor construction and initialization. No Petari adaptation or inferred class identity. Actor_8024C8D0 integer argument/field refinement preserves native instructions and adds zero counted gain. Evidence: work/create-506f0/findings.md and verification.json.

## Refresh_80250790

Direct SB4E01 retail reconstruction of fn_80250790,308 exact source-integrated bytes: three-child record/value refresh. Helper output sizes checked from native call chains; no inferred original identity or Petari adaptation. Untagged value setter accepts full32-bit input and stores low16, preserving all support instructions with zero extra gain. Evidence: work/refresh-50790/findings.md and verification.json.

## Control_802508D0

Direct SB4E01 retail reconstruction of fn_802508D0, fn_80250930, fn_80250990 and fn_802509F0: group update, movement controls and child-array setup,408 exact source-integrated bytes. No Petari adaptation or inferred original class identity. Evidence: work/control-508d0/findings.md and verification.json.

## Rotation_80250A70

Direct SB4E01 retail reconstruction of fn_80250A70,600 exact source-integrated bytes: child rotation matrix transform and angle extraction. Shared constants retain existing ownership; weak vector helpers excluded from gain. No Petari adaptation or inferred class identity. Evidence: work/rotation-50a70/findings.md and verification.json.

## Matrix_80250CD0

Direct SB4E01 retail reconstruction of fn_80250CD0,328 exact source-integrated bytes: three-angle matrix construction with native double-call/float-rounding sequence and exact arithmetic schedule. No Petari adaptation, injected assembly or inferred original class identity. Evidence: work/matrix-50cd0/findings.md and verification.json.

## Query_80250E20

Direct SB4E01 retail reconstruction of fn_80250E20, fn_80250E80, fn_80250EF0 and fn_80250F50,396 exact source-integrated bytes: record value/identity queries and signed threshold conjunction. Native unsigned boundary behavior and distinct helper success checks preserved. No Petari adaptation or inferred class identity. Evidence: work/query-50e20/findings.md and verification.json.

## Value_80250FC0

Direct SB4E01 retail reconstruction of fn_80250FC0, fn_80251050 and fn_802510B0,444 exact source-integrated bytes. Query EF0 u16 return refinement preserves all support instructions with zero extra gain. No Petari adaptation or inferred class identity. Evidence: work/value-50fc0/findings.md and verification.json.

## Query_80251180

Direct SB4E01 retail reconstruction of fn_80251180, fn_802511B0, fn_80251220 and fn_802512B0,424 exact source-integrated bytes: record queries, group exclusion check and shared-pool string dispatch. No Petari adaptation or inferred class identity; pool ownership unchanged. Evidence: work/query-51180/findings.md and verification.json.

## Control_80251340 and Group_80251430

Direct SB4E01 retail reconstruction: fn_80251340 adds240 exact source-integrated bytes; fn_80251430 remains an original-linked partial with71/90 instruction positions exact and zero integration gain. Native interfaces and stack layout reconstructed without Petari adaptation or inferred class identity. Evidence: work/control-51340/findings.md and verification.json.

## Setup_802515A0 and State_80251690

Direct SB4E01 retail reconstruction of fn_802515A0, fn_80251690 and fn_80251790 (236 + 248 + 192 = 676 bytes). Opaque actor fields, shared name-pool offsets, native virtual dispatch and nerve transitions follow retail instruction evidence. All instructions and relocation symbols match; linked dependency addresses, source ownership, incoming exact functions, native padding and complete retail DOL identity verified. No external implementation used. No data ownership gain; Group_80251430 remains an original-linked partial. See work/Setup-515a0/findings.md and verification.json for acceptance evidence.

## State_80251850

Direct retail reconstruction of fn_80251850, fn_802518E0, fn_80251980 and fn_802519E0: 492 exact source-integrated bytes. Opaque observed actor offsets and original dependency symbols retained. Four-byte shared string declaration reproduces native small-data addressing; following original fn_80251A50 alignment preserves retail padding. No data ownership gain or external implementation. All instructions/relocations, linked dependencies, source selection, incoming regressions and full retail DOL identity verified. Evidence: work/state-51850.

## State_80251A50

Direct SB4E01 retail reconstruction of fn_80251A50, fn_80251AB0 and fn_80251B50 (340 bytes / 85 exact instructions). Observed opaque layouts and native first-step branches, helper order and nerve symbols preserved; no external implementation or data ownership gain. Following original fn_80251BB0 alignment preserves native padding. Instruction relocations, linked dependencies, source selection, incoming regressions and full retail DOL identity verified. Group_80251430 remains original-linked. Evidence: work/state-51a50.

## State_80251BB0

Direct SB4E01 retail reconstruction of fn_80251BB0 and fn_80251C40: 260 bytes / 65 exact instructions. Observed actor/child offsets, stack argument, native branches and nerve symbols preserved. No external implementation or data ownership gain. Following original fn_80251CC0 alignment preserves native padding. Instruction relocations, linked dependencies, source selection, incoming regressions and full retail DOL identity verified. Evidence: work/state-51bb0.

## State_80251CC0

Direct SB4E01 retail reconstruction of fn_80251CC0: 264 bytes / 66 exact instructions. Native virtual appear calls, step-90 branch, cleanup order, observed opaque fields and nerve symbols retained. No external implementation or data ownership gain. Following original fn_80251DD0 alignment preserves retail padding. Instruction relocations, linked dependencies, source selection, incoming regressions and full retail DOL identity verified. Evidence: work/state-51cc0.

## State_80251DD0

Direct SB4E01 retail reconstruction of fn_80251DD0: 128 bytes / 32 exact instructions. Native first-step helper order, two shared-name calls with argument 30, actor byte clear and nerve transition retained. No external implementation, compiler helper or data ownership gain. Instruction relocations, linked dependencies, source selection, incoming regressions and full retail DOL identity verified. Evidence: work/state-51dd0.

## State_80251E50

Direct SB4E01 retail reconstruction of fn_80251E50: 212 bytes / 53 exact instructions. Observed actor offsets, tagged value, native wide-character copyString dependency and helper sequence retained. Initial u16 declaration corrected to wchar_t before integration. No external implementation, compiler helper or data ownership gain. Instruction relocations, linked dependencies, source selection, incoming regressions, padding and full retail DOL identity verified. Evidence: work/state-51e50.

## State_80251F30

Direct SB4E01 retail reconstruction of fn_80251F30: 104 bytes / 26 exact instructions. Native first-step helper order, child predicate and nerve transition retained. No external implementation or data ownership gain. Instruction relocations, linked dependencies, source selection, incoming regressions, following original alignment and full retail DOL identity verified. Evidence: work/state-51f30.

## State_80251FA0

Direct SB4E01 retail reconstruction of fn_80251FA0: 232 bytes / 58 exact instructions. Native shared pool offsets, childA8 setup, predicate order and both nerve branches retained. No external implementation, compiler helper or data ownership gain. Instruction relocations, linked dependencies, source selection, incoming regressions, following original alignment and full retail DOL identity verified. Evidence: work/state-51fa0.

## State_80252090

Direct SB4E01 retail reconstruction of fn_80252090: 140 bytes / 35 exact instructions. Native child field144 first-step call, predicate order and both nerve transitions retained. No external implementation or data ownership gain. Instruction relocations, linked dependencies, source selection, incoming regressions, following original alignment and full retail DOL identity verified. Evidence: work/state-52090.

## State_80252120

Direct SB4E01 retail reconstruction of fn_80252120: 88 bytes / 22 exact instructions. Retains ignored isFirstStep result, ordered child predicates and nerve transition. No external implementation or data ownership gain. Instruction relocations, linked dependencies, source selection, incoming regressions, following original alignment and full retail DOL identity verified. Evidence: work/state-52120.

## State_80252180

Direct SB4E01 retail reconstruction of fn_80252180: 236 bytes / 59 exact instructions. Native tagged temporary, observed actor offsets, record indexing, virtual appear and nerve transition retained. No external implementation or data ownership gain. Instruction relocations, linked dependencies, source selection, incoming regressions, following original alignment and full retail DOL identity verified. Evidence: work/state-52180.

## State_80252270

Direct SB4E01 retail reconstruction of fn_80252270: 320 bytes / 80 exact instructions. Native first-step appear condition, ordered predicates, sound calls and three distinct nerve transitions retained. No external implementation or data ownership gain. Instruction relocations, linked dependencies, source selection, incoming regressions and full retail DOL identity verified. Evidence: work/state-52270.

## State_802523B0

Direct SB4E01 retail reconstruction of fn_802523B0: 84 bytes / 21 exact instructions. Native ordered child predicates and nerve transition retained. No external implementation or data ownership gain. Instruction relocations, linked dependencies, source selection, incoming regressions, following original alignment and full retail DOL identity verified. Evidence: work/state-523b0.

## State_80252410

Direct SB4E01 retail reconstruction of fn_80252410: 84 bytes / 21 exact instructions. Native ordered child predicates and distinct nerve 807D35BC retained. No external implementation or data ownership gain. Instruction relocations, linked dependencies, source selection, incoming regressions, following original alignment and full retail DOL identity verified. Evidence: work/state-52410.

## State_80252470

Direct SB4E01 retail reconstruction of fn_80252470: 408 bytes / 102 exact instructions. Native shared pool offsets, tagged value and name buffer, repeated predicate calls and three nerve transitions retained. No external implementation or data ownership gain. Instruction relocations, linked dependencies, source selection, incoming regressions, following original alignment and full retail DOL identity verified. Evidence: work/state-52470.

## State_80252610

Direct SB4E01 retail reconstruction of fn_80252610: 124 bytes / 31 exact instructions. Native tagged temporary, observed actor fields, helper sequence and nerve transition retained. No external implementation or data ownership gain. Instruction relocations, linked dependencies, source selection, incoming regressions, following original alignment and full retail DOL identity verified. Evidence: work/state-52610.

## State_80252690

Direct SB4E01 retail reconstruction of fn_80252690: 212 bytes / 53 exact instructions. Native tagged temporaries, saved child index, flag load and nerve selection retained. No external implementation or data ownership gain. Instruction relocations, linked dependencies, source selection, incoming regressions, following original alignment and full retail DOL identity verified. Evidence: work/state-52690.

## State_80252770

Direct SB4E01 retail reconstruction of fn_80252770: 136 bytes / 34 exact instructions. Native child setup, shared string references and nerve transition retained. No external implementation or data ownership gain. Instruction relocations, linked dependencies, source selection, incoming regressions, following original alignment and full retail DOL identity verified. Evidence: work/state-52770.

## State_80252800

Direct SB4E01 retail reconstruction of fn_80252800: 84 bytes / 21 exact instructions. Native ordered child predicates and distinct nerve 807D35D0 retained. No external implementation or data ownership gain. Instruction relocations, linked dependencies, source selection, incoming regressions, following original alignment and full retail DOL identity verified. Evidence: work/state-52800.

## State_80252860

Direct SB4E01 retail reconstruction of fn_80252860: 148 bytes / 37 exact instructions. Native setup, shared strings, flag and nerve branches retained. No external implementation or data ownership gain. Relocations, linked dependencies, source selection, regressions, padding and retail DOL identity verified. Evidence: work/state-52860.
